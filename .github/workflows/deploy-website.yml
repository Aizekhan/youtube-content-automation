name: Deploy Website to n8n-creator.space

on:
  push:
    branches:
      - master
    paths:
      - 'prompts-editor.html'
      - 'channel-configs.html'
      - '*.html'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy HTML files to nginx server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy prompts-editor.html via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          source: "prompts-editor.html"
          target: ${{ secrets.SSH_WEB_PATH }}
          strip_components: 0

      - name: Set file permissions
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ${{ secrets.SSH_WEB_PATH }}
            chmod 644 prompts-editor.html
            chown www-data:www-data prompts-editor.html 2>/dev/null || chown nginx:nginx prompts-editor.html 2>/dev/null || true
            echo "✅ prompts-editor.html deployed successfully"
            ls -lh prompts-editor.html

      - name: Deployment Summary
        run: |
          echo "🚀 Website deployment completed!"
          echo "- File: prompts-editor.html"
          echo "- Server: ${{ secrets.SSH_HOST }}"
          echo "- URL: https://n8n-creator.space/prompts-editor.html"
