name: Deploy Website to n8n-creator.space

on:
  push:
    branches:
      - master
    paths:
      - 'prompts-editor.html'
      - 'channel-configs.html'
      - '*.html'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy HTML files to nginx server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy HTML files via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          source: "prompts-editor.html,channel-configs.html"
          target: ${{ secrets.SSH_WEB_PATH }}
          strip_components: 0

      - name: Set file permissions
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ${{ secrets.SSH_WEB_PATH }}
            chmod 644 prompts-editor.html channel-configs.html
            chown www-data:www-data prompts-editor.html channel-configs.html 2>/dev/null || chown nginx:nginx prompts-editor.html channel-configs.html 2>/dev/null || true
            echo "✅ HTML files deployed successfully"
            ls -lh prompts-editor.html channel-configs.html

      - name: Deployment Summary
        run: |
          echo "🚀 Website deployment completed!"
          echo "- Files: prompts-editor.html, channel-configs.html"
          echo "- Server: ${{ secrets.SSH_HOST }}"
          echo "- URLs:"
          echo "  - https://n8n-creator.space/prompts-editor.html"
          echo "  - https://n8n-creator.space/channel-configs.html"
