<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Prompts - n8n Creator Space</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="css/unified-navigation.css">
    <style>
        :root {
            --primary-color: #667eea;
            --success-color: #10b981;
            --danger-color: #ef4444;
            --warning-color: #f59e0b;
            --dark-bg: #1a1d2e;
            --card-bg: #22263a;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            min-height: 100vh;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            color: #e5e7eb;
            padding-bottom: 2rem;
        }

        .main-container {
            padding: 30px 0;
        }

        .header-section {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(102, 126, 234, 0.2);
        }

        .header-section h1 {
            font-size: 2rem;
            font-weight: 700;
            color: #e5e7eb;
            margin-bottom: 8px;
        }

        .header-section p {
            color: rgba(226, 232, 240, 0.6);
            margin: 0;
        }

        .view-tabs {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 20px 20px 0 20px;
            margin-bottom: 30px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(102, 126, 234, 0.2);
        }

        .nav-pills .nav-link {
            border-radius: 12px;
            padding: 12px 24px;
            color: rgba(226, 232, 240, 0.7);
            font-weight: 500;
            transition: all 0.3s;
            border: 1px solid transparent;
        }

        .nav-pills .nav-link:hover {
            background: rgba(102, 126, 234, 0.15);
            color: #e5e7eb;
            border-color: rgba(102, 126, 234, 0.3);
        }

        .nav-pills .nav-link.active {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            font-weight: 600;
            box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);
        }

        /* Template Type Panes */
        .template-type-pane {
            display: none;
        }
        .template-type-pane.active {
            display: block;
        }

        .content-area {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(102, 126, 234, 0.2);
            min-height: 600px;
        }

        /* Template Cards (Library View) */
        .template-card {
            background: rgba(34, 38, 58, 0.6);
            border: 2px solid rgba(102, 126, 234, 0.3);
            border-radius: 12px;
            padding: 24px;
            transition: all 0.3s;
            cursor: pointer;
            height: 100%;
        }

        .template-card:hover {
            border-color: var(--primary-color);
            box-shadow: 0 10px 15px -3px rgba(102, 126, 234, 0.4);
            transform: translateY(-2px);
            background: rgba(34, 38, 58, 0.8);
        }

        .template-card.active {
            border-color: var(--success-color);
            background: rgba(16, 185, 129, 0.15);
        }

        .template-card.inactive {
            opacity: 0.5;
            background: rgba(34, 38, 58, 0.4);
        }

        .template-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
        }

        .badge-active {
            background: rgba(16, 185, 129, 0.2);
            color: #10b981;
            border: 1px solid rgba(16, 185, 129, 0.3);
        }

        .badge-draft {
            background: rgba(245, 158, 11, 0.2);
            color: #f59e0b;
            border: 1px solid rgba(245, 158, 11, 0.3);
        }

        .badge-inactive {
            background: rgba(239, 68, 68, 0.2);
            color: #ef4444;
            border: 1px solid rgba(239, 68, 68, 0.3);
        }

        /* Editor Form */
        .section-card {
            background: rgba(34, 38, 58, 0.4);
            border: 1px solid rgba(102, 126, 234, 0.3);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .section-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 16px;
        }

        .section-title {
            font-size: 1.125rem;
            font-weight: 600;
            color: #e5e7eb;
            margin: 0;
        }

        .rule-item {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 12px;
            padding: 12px;
            background: rgba(15, 23, 42, 0.5);
            border-radius: 8px;
            border: 1px solid rgba(102, 126, 234, 0.2);
        }

        .rule-item input {
            flex: 1;
        }

        .btn-remove-rule {
            background: rgba(239, 68, 68, 0.2);
            color: #ef4444;
            border: 1px solid rgba(239, 68, 68, 0.3);
            padding: 6px 12px;
            border-radius: 6px;
            transition: all 0.2s;
        }

        .btn-remove-rule:hover {
            background: rgba(239, 68, 68, 0.3);
        }

        .btn-add-rule {
            width: 100%;
            padding: 12px;
            border: 2px dashed rgba(102, 126, 234, 0.3);
            background: transparent;
            color: rgba(226, 232, 240, 0.7);
            border-radius: 8px;
            transition: all 0.3s;
        }

        .btn-add-rule:hover {
            border-color: var(--primary-color);
            color: var(--primary-color);
            background: rgba(102, 126, 234, 0.1);
        }

        /* Preview Tab */
        .preview-container {
            background: #1f2937;
            color: #f9fafb;
            padding: 24px;
            border-radius: 12px;
            font-family: 'Monaco', 'Menlo', monospace;
            font-size: 0.875rem;
            line-height: 1.6;
            max-height: 600px;
            overflow-y: auto;
        }

        .preview-section {
            margin-bottom: 24px;
        }

        .preview-label {
            color: #60a5fa;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .preview-content {
            background: #111827;
            padding: 16px;
            border-radius: 8px;
            white-space: pre-wrap;
            word-wrap: break-word;
        }

        /* Version History */
        .version-item {
            border-left: 3px solid rgba(102, 126, 234, 0.3);
            padding-left: 20px;
            margin-bottom: 24px;
            position: relative;
        }

        .version-item.current {
            border-left-color: var(--success-color);
        }

        .version-item::before {
            content: '';
            position: absolute;
            left: -8px;
            top: 6px;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: rgba(102, 126, 234, 0.5);
        }

        .version-item.current::before {
            background: var(--success-color);
        }

        .version-number {
            font-weight: 700;
            color: #e5e7eb;
            font-size: 1.125rem;
        }

        .version-meta {
            color: rgba(226, 232, 240, 0.6);
            font-size: 0.875rem;
            margin: 4px 0;
        }

        .version-changes {
            background: rgba(15, 23, 42, 0.5);
            border: 1px solid rgba(102, 126, 234, 0.2);
            padding: 12px;
            border-radius: 8px;
            margin-top: 8px;
            color: rgba(226, 232, 240, 0.8);
        }

        /* Stats cards */
        .stat-card {
            text-align: center;
            padding: 16px;
            background: rgba(34, 38, 58, 0.6);
            border: 1px solid rgba(102, 126, 234, 0.3);
            border-radius: 12px;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: #e5e7eb;
        }

        .stat-label {
            color: rgba(226, 232, 240, 0.6);
            font-size: 0.875rem;
            margin-top: 4px;
        }

        /* Loading spinner */
        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid #f3f4f6;
            border-radius: 50%;
            border-top-color: var(--primary-color);
            animation: spin 0.8s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Toast notifications */
        .toast-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
        }

        /* Modal Window Styles - Dark text on white background */
        .modal-content {
            background-color: #ffffff;
            color: #1a1d2e;
        }

        .modal-header {
            background-color: #f8f9fa;
            border-bottom: 1px solid #dee2e6;
        }

        .modal-title {
            color: #1a1d2e;
            font-weight: 600;
        }

        .modal-body {
            background-color: #ffffff;
            color: #1a1d2e;
        }

        .modal-footer {
            background-color: #f8f9fa;
            border-top: 1px solid #dee2e6;
        }

        /* Form elements in modal - dark text */
        .modal .form-label {
            color: #1a1d2e;
            font-weight: 500;
        }

        .modal .form-control,
        .modal .form-select {
            background-color: #ffffff;
            color: #1a1d2e;
            border: 1px solid #ced4da;
        }

        .modal .form-control:focus,
        .modal .form-select:focus {
            background-color: #ffffff;
            color: #1a1d2e;
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }

        .modal .form-control::placeholder {
            color: #6c757d;
        }

        .modal textarea.form-control {
            background-color: #ffffff;
            color: #1a1d2e;
        }

        /* Section cards in modal */
        .modal .section-card {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
        }

        .modal .section-title {
            color: #1a1d2e;
        }

        .modal .rule-item {
            background: #ffffff;
            border: 1px solid #dee2e6;
        }

        .modal .rule-item input {
            background-color: #ffffff;
            color: #1a1d2e;
        }

        /* Better readability for template card descriptions */
        .template-card .text-muted {
            color: rgba(226, 232, 240, 0.85) !important;
            font-size: 0.95rem !important;
        }

        .template-card .small {
            font-size: 0.9rem !important;
        }

        .template-card h5 {
            color: #e5e7eb;
            font-weight: 600;
        }

        /* Make all text-muted more readable on dark background */
        .text-muted {
            color: rgba(226, 232, 240, 0.75) !important;
        }

        /* Headings in modal */
        .modal h3, .modal h4, .modal h5, .modal h6 {
            color: #1a1d2e;
        }

        /* Hide editor buttons when form is inside modal */
        .modal .editor-header-buttons,
        .modal .editor-footer-buttons {
            display: none !important;
        }

        /* Also hide the editorTitle when in modal (we have modal title) */
        .modal #editorTitle {
            display: none !important;
        }

        /* Override Bootstrap text-secondary to be light on dark background */
        .text-secondary {
            color: #d1d5db !important;
        }

        .form-label {
            color: #e5e7eb;
        }

        .card-header {
            color: #ffffff;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <!-- Unified Navigation (loaded from js/navigation.js) -->
    <div id="navigation-placeholder"></div>

    <div class="container main-container">
        <!-- Header -->
        <div class="header-section">
            <div>
                <h1><i class="bi bi-brain"></i> AI Prompts Template System v2</h1>
                <p>Manage prompt templates with version control and structured editing</p>
                <div class="alert alert-info mt-3" style="background: rgba(59, 130, 246, 0.1); border: 1px solid rgba(59, 130, 246, 0.3); color: #93c5fd;">
                    <i class="bi bi-info-circle"></i> <strong>Що таке шаблон?</strong><br>
                    Шаблон визначає <strong>ЯК</strong> AI думає і генерує контент (поведінка, структура відповіді, правила).
                    <strong>ПРО ЩО</strong> генерувати контент (тема, жанр, тон, стиль) визначається в налаштуваннях каналу.
                    Один шаблон може використовуватись багатьма каналами.
                </div>
            </div>
        </div>

        <!-- Navigation Tabs -->
        <!-- Navigation tabs removed - each template type has its own section -->

        <!-- Content Area -->
        <div class="content-area">
            <div class="tab-content" id="mainTabContent">

                <!-- Template Library View -->
                <div class="tab-pane fade show active" id="library" role="tabpanel">
                    <h3 class="mb-4">Template Library</h3>

                    <!-- Template Type Sub-tabs -->
                    <ul class="nav nav-pills mb-4" id="templateTypeTabs" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active" id="theme-templates-tab" onclick="switchTemplateType('theme')" type="button">
                                <i class="bi bi-lightbulb"></i> Theme Templates
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="narrative-templates-tab" onclick="switchTemplateType('narrative')" type="button">
                                <i class="bi bi-file-text"></i> Narrative Templates
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="image-templates-tab" onclick="switchTemplateType('image')" type="button">
                                <i class="bi bi-image"></i> Image Generation
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="video-templates-tab" onclick="switchTemplateType('video')" type="button">
                                <i class="bi bi-film"></i> Video Editing
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="cta-templates-tab" onclick="switchTemplateType('cta')" type="button">
                                <i class="bi bi-megaphone"></i> CTA Templates
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="tts-templates-tab" onclick="switchTemplateType('tts')" type="button">
                                <i class="bi bi-mic"></i> TTS/Voice
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="sfx-templates-tab" onclick="switchTemplateType('sfx')" type="button">
                                <i class="bi bi-soundwave"></i> SFX
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="thumbnail-templates-tab" onclick="switchTemplateType('thumbnail')" type="button">
                                <i class="bi bi-card-image"></i> Thumbnail
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="description-templates-tab" onclick="switchTemplateType('description')" type="button">
                                <i class="bi bi-card-text"></i> Description
                            </button>
                        </li>
                    </ul>

                    <!-- Template Type Content Areas -->
                    <div id="templateTypeContent">

                        <!-- Theme Templates -->
                        <div class="template-type-pane active" id="theme-templates-pane">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">Theme Templates</h5>
                                    <p class="text-muted small mb-0">Generate video topic ideas and themes</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('theme')">
                                    <i class="bi bi-plus-circle"></i> New Theme Template
                                </button>
                            </div>
                            <div id="themeTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <div class="loading-spinner"></div>
                                    <p class="text-muted mt-3">Loading theme templates...</p>
                                </div>
                            </div>
                        </div>

                        <!-- Narrative Templates -->
                        <div class="template-type-pane" id="narrative-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">Narrative Templates</h5>
                                    <p class="text-muted small mb-0">Structure and write video scripts with SSML</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('narrative')">
                                    <i class="bi bi-plus-circle"></i> New Narrative Template
                                </button>
                            </div>
                            <div id="narrativeTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <div class="loading-spinner"></div>
                                    <p class="text-muted mt-3">Loading narrative templates...</p>
                                </div>
                            </div>
                        </div>

                        <!-- Image Generation Templates -->
                        <div class="template-type-pane" id="image-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">Image Generation Templates</h5>
                                    <p class="text-muted small mb-0">AI prompts for generating visuals (DALL-E, Midjourney, Stable Diffusion)</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('image')">
                                    <i class="bi bi-plus-circle"></i> New Image Template
                                </button>
                            </div>

                            <!-- EC2 SD35 GPU Instance Control Panel -->
                            <div id="ec2SD35ControlPanel" style="margin-bottom: 24px; padding: 20px; background: linear-gradient(135deg, #10b981 0%, #059669 100%); border-radius: 12px; color: white; box-shadow: 0 8px 16px rgba(16, 185, 129, 0.3);">
                                <h4 style="margin: 0 0 8px 0; font-size: 18px; display: flex; align-items: center; gap: 8px;">
                                    <span>🚀</span> EC2 SD 3.5 Medium Instance (g5.xlarge)
                                </h4>
                                <div style="font-size: 13px; opacity: 0.9; margin-bottom: 16px;">
                                    NVIDIA A10G 24GB • SD 3.5 Medium • 9.9GB VRAM • 42s/image
                                </div>

                                <!-- Status Display -->
                                <div style="background: rgba(255,255,255,0.15); border-radius: 8px; padding: 16px; margin-bottom: 16px;">
                                    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 12px; font-size: 14px;">
                                        <div>
                                            <div style="opacity: 0.85; margin-bottom: 4px; font-size: 12px;">Статус:</div>
                                            <div id="ec2SD35Status" style="font-weight: bold; font-size: 16px;">⏳ Завантаження...</div>
                                        </div>
                                        <div>
                                            <div style="opacity: 0.85; margin-bottom: 4px; font-size: 12px;">Instance ID:</div>
                                            <div id="ec2SD35InstanceId" style="font-weight: 600; font-size: 14px; font-family: monospace;">--</div>
                                        </div>
                                        <div>
                                            <div style="opacity: 0.85; margin-bottom: 4px; font-size: 12px;">Вартість/год:</div>
                                            <div id="ec2SD35CostPerHour" style="font-weight: bold; font-size: 16px; color: #fde047;">$1.006</div>
                                        </div>
                                        <div>
                                            <div style="opacity: 0.85; margin-bottom: 4px; font-size: 12px;">Public IP:</div>
                                            <div id="ec2SD35PublicIp" style="font-weight: 600; font-size: 14px; font-family: monospace;">--</div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Endpoint Info (when running) -->
                                <div id="ec2SD35EndpointInfo" style="display: none; background: rgba(255,255,255,0.15); border-radius: 8px; padding: 14px; margin-bottom: 16px;">
                                    <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 8px;">
                                        <span style="font-weight: 600; font-size: 14px;">🌐 API Endpoint:</span>
                                        <div style="font-family: monospace; font-size: 13px; background: rgba(0,0,0,0.2); padding: 4px 12px; border-radius: 4px; flex: 1;">
                                            <span id="ec2SD35Endpoint">--</span>
                                        </div>
                                        <button onclick="copyToClipboard(document.getElementById('ec2SD35Endpoint').textContent)" style="padding: 6px 12px; background: rgba(255,255,255,0.2); border: none; border-radius: 4px; color: white; cursor: pointer; font-size: 12px;">
                                            📋 Copy
                                        </button>
                                    </div>
                                    <div style="font-size: 12px; opacity: 0.8;">
                                        ✅ Ready for image generation • POST /generate • Health check: GET /health
                                    </div>
                                </div>

                                <!-- Control Buttons -->
                                <div style="display: flex; gap: 12px; flex-wrap: wrap;">
                                    <button type="button" id="ec2SD35StartBtn" onclick="startEC2SD35Instance()" style="padding: 12px 24px; background: rgba(255, 255, 255, 0.95); color: #059669; border: none; border-radius: 8px; font-weight: 700; cursor: pointer; transition: all 0.2s; box-shadow: 0 2px 8px rgba(0,0,0,0.2);">
                                        ▶️ Start Instance
                                    </button>
                                    <button type="button" id="ec2SD35StopBtn" onclick="stopEC2SD35Instance()" style="padding: 12px 24px; background: rgba(239, 68, 68, 0.95); color: white; border: none; border-radius: 8px; font-weight: 700; cursor: pointer; transition: all 0.2s; box-shadow: 0 2px 8px rgba(0,0,0,0.2);">
                                        ⏹️ Stop Instance
                                    </button>
                                    <button type="button" id="ec2SD35RefreshBtn" onclick="refreshEC2SD35Status()" style="padding: 12px 24px; background: rgba(255,255,255,0.2); color: white; border: 1px solid rgba(255,255,255,0.4); border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.2s;">
                                        🔄 Refresh
                                    </button>
                                </div>

                                <!-- Info Alert -->
                                <div style="margin-top: 16px; padding: 12px; background: rgba(255,255,255,0.12); border-radius: 8px; font-size: 13px; border-left: 3px solid #fde047;">
                                    <div style="opacity: 0.95;">
                                        💡 <strong>Auto-shutdown:</strong> Instance автоматично зупиняється через 5 хв бездіяльності.<br>
                                        💰 <strong>Економія:</strong> Запускайте тільки коли потрібно генерувати зображення.
                                    </div>
                                </div>
                            </div>
                            <div id="imageTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <i class="bi bi-stars" style="font-size: 3rem; color: rgba(102, 126, 234, 0.3);"></i>
                                    <p class="text-muted mt-3">No image generation templates yet</p>
                                    <button class="btn btn-outline-primary mt-2" onclick="createNewTemplate('image')">
                                        Create First Template
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Video Editing Templates -->
                        <div class="template-type-pane" id="video-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">Video Editing Templates</h5>
                                    <p class="text-muted small mb-0">Transitions, effects, pacing, and editing parameters</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('video')">
                                    <i class="bi bi-plus-circle"></i> New Video Template
                                </button>
                            </div>
                            <div id="videoTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <i class="bi bi-film" style="font-size: 3rem; color: rgba(102, 126, 234, 0.3);"></i>
                                    <p class="text-muted mt-3">No video editing templates yet</p>
                                    <button class="btn btn-outline-primary mt-2" onclick="createNewTemplate('video')">
                                        Create First Template
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- CTA Templates -->
                        <div class="template-type-pane" id="cta-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">CTA Templates</h5>
                                    <p class="text-muted small mb-0">Call-to-action overlays and end screens</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('cta')">
                                    <i class="bi bi-plus-circle"></i> New CTA Template
                                </button>
                            </div>
                            <div id="ctaTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <i class="bi bi-megaphone" style="font-size: 3rem; color: rgba(102, 126, 234, 0.3);"></i>
                                    <p class="text-muted mt-3">No CTA templates yet</p>
                                    <button class="btn btn-outline-primary mt-2" onclick="createNewTemplate('cta')">
                                        Create First Template
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- TTS/Voice Templates -->
                        <div class="template-type-pane" id="tts-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">TTS/Voice Templates</h5>
                                    <p class="text-muted small mb-0">Text-to-speech voice selection and service configuration</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('tts')">
                                    <i class="bi bi-plus-circle"></i> New TTS/Voice Template
                                </button>
                            </div>
                            <div id="ttsTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <div class="loading-spinner"></div>
                                    <p class="text-muted mt-3">Loading TTS templates...</p>
                                </div>
                            </div>
                        </div>

                        <!-- SFX Templates -->
                        <div class="template-type-pane" id="sfx-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">SFX Templates</h5>
                                    <p class="text-muted small mb-0">Sound effects and background music configuration</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('sfx')">
                                    <i class="bi bi-plus-circle"></i> New SFX Template
                                </button>
                            </div>
                            <div id="sfxTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <div class="loading-spinner"></div>
                                    <p class="text-muted mt-3">Loading SFX templates...</p>
                                </div>
                            </div>
                        </div>

                        <!-- Thumbnail Templates -->
                        <div class="template-type-pane" id="thumbnail-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">Thumbnail Templates</h5>
                                    <p class="text-muted small mb-0">YouTube thumbnail design with CTR optimization</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('thumbnail')">
                                    <i class="bi bi-plus-circle"></i> New Thumbnail Template
                                </button>
                            </div>
                            <div id="thumbnailTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <div class="loading-spinner"></div>
                                    <p class="text-muted mt-3">Loading Thumbnail templates...</p>
                                </div>
                            </div>
                        </div>

                        <!-- Description Templates -->
                        <div class="template-type-pane" id="description-templates-pane" style="display: none;">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div>
                                    <h5 class="mb-1">Description Templates</h5>
                                    <p class="text-muted small mb-0">Video descriptions with SEO keywords and tags</p>
                                </div>
                                <button class="btn btn-primary" onclick="createNewTemplate('description')">
                                    <i class="bi bi-plus-circle"></i> New Description Template
                                </button>
                            </div>
                            <div id="descriptionTemplatesGrid" class="row g-4">
                                <div class="col-12 text-center py-5">
                                    <div class="loading-spinner"></div>
                                    <p class="text-muted mt-3">Loading Description templates...</p>
                                </div>
                            </div>
                        </div>


                    </div>
                </div>

                <!-- Editor View -->
                <div class="tab-pane fade" id="editor" role="tabpanel">
                    <div class="d-flex justify-content-between align-items-center mb-4 editor-header-buttons">
                        <h3 id="editorTitle">Edit Template</h3>
                        <div>
                            <button class="btn btn-outline-secondary me-2" onclick="backToLibrary()">
                                <i class="bi bi-arrow-left"></i> Back
                            </button>
                            <button class="btn btn-success" onclick="saveTemplate()">
                                <i class="bi bi-save"></i> Save Changes
                            </button>
                        </div>
                    </div>

                    <form id="templateForm">
                        <!-- Basic Info Section -->
                        <div class="section-card">
                            <div class="section-header">
                                <h5 class="section-title"><i class="bi bi-info-circle"></i> Basic Information</h5>
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label">Template Name *</label>
                                    <input type="text" class="form-control" id="template_name" required>
                                </div>
                                <div class="col-md-3" id="agent_type_field">
                                    <label class="form-label">Agent Type *</label>
                                    <select class="form-select" id="agent_type">
                                        <option value="narrative_architect">Narrative Architect</option>
                                        <option value="theme_agent">Theme Agent</option>
                                        <option value="custom">Custom</option>
                                    </select>
                                </div>
                                <div class="col-12">
                                    <label class="form-label">Description</label>
                                    <textarea class="form-control" id="description" rows="2"></textarea>
                                </div>
                            </div>
                        </div>

                        <!-- Fields for Theme & Narrative Templates -->
                        <div class="template-fields-group" id="theme-narrative-fields" style="display: none;">
                            <!-- Model Configuration -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-cpu"></i> Model Configuration</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label">Model</label>
                                        <select class="form-select" id="model">
                                            <option value="gpt-4o">GPT-4o</option>
                                            <option value="gpt-4o-mini">GPT-4o Mini</option>
                                            <option value="gpt-4-turbo">GPT-4 Turbo</option>
                                        </select>
                                        <small class="text-muted">Модель OpenAI для генерації контенту</small>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Temperature</label>
                                        <input type="number" class="form-control" id="temperature" min="0" max="2" step="0.1" value="0.8">
                                        <small class="text-muted">Креативність (0=точність, 2=креативність)</small>
                                    </div>
                                </div>
                                <div class="alert alert-info mt-3">
                                    <i class="bi bi-info-circle"></i> <strong>Max Tokens</strong> налаштовується окремо для кожного каналу в <strong>Channel Config</strong> (максимум 16000)
                                </div>
                            </div>

                            <!-- Role Definition -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-person-badge"></i> Role Definition</h5>
                                </div>
                                <textarea class="form-control" id="role_definition" rows="4" placeholder="You are Narrative Architect..."></textarea>
                            </div>

                            <!-- Core Rules -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-list-check"></i> Core Rules</h5>
                                </div>
                                <div id="coreRulesList">
                                    <!-- Rules will be added here dynamically -->
                                </div>
                                <button type="button" class="btn-add-rule" onclick="addCoreRule()">
                                    <i class="bi bi-plus-circle"></i> Add Rule
                                </button>
                            </div>
                        </div>

                                                <!-- Fields for Image Generation Template -->
                        <div class="template-fields-group" id="image-fields" style="display: none;">
                            <!-- AI Configuration -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-robot"></i> Visual Director AI Instructions</h5>
                                    <p class="text-muted small mb-0">AI інструкції які контролюють ЯК AI створює промпти для зображень. Технічні параметри (provider, розміри) знаходяться в налаштуваннях каналу.</p>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Role Definition *</label>
                                    <textarea class="form-control" id="image_role_definition" rows="4" placeholder="You are a Visual Director specialized in creating cinematic, atmospheric image prompts for ancient mythology content...">You are a Visual Director for visual storytelling content.
You create highly detailed, cinematic image prompts that capture atmosphere and emotion.
Your prompts are painterly, evocative, and designed for AI image generation.</textarea>
                                    <small class="text-muted">Опишіть роль AI як Visual Director для створення image prompts</small>
                                </div>

                                <!-- Core Rules -->
                                <div class="mb-3">
                                    <label class="form-label">Core Rules for Image Prompt Creation *</label>
                                    <div id="image_core_rules_container">
                                        <!-- Dynamic list of rules -->
                                    </div>
                                    <button type="button" class="btn btn-sm btn-outline-primary mt-2" onclick="addImageCoreRule()">
                                        <i class="bi bi-plus-circle"></i> Add Rule
                                    </button>
                                    <small class="form-text text-muted d-block mt-1">Правила які AI має дотримуватись при створенні image prompts</small>
                                </div>
                            </div>

                            
                            <!-- Provider Settings (General for all channels) -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-cloud"></i> Image Provider Settings</h5>
                                    <p class="text-muted small mb-0">Загальні налаштування провайдера для генерації зображень. Візуальний стиль (keywords, atmosphere, colors) налаштовується в Channel Config.</p>
                                </div>

                                <!-- Provider Selection -->
                                <div class="mb-3">
                                    <label class="form-label">Image Provider *</label>
                                    <select class="form-select" id="image_provider">
                                        <option value="ec2-sd35">EC2 Stable Diffusion 3.5 (Current)</option>
                                        <option value="aws-bedrock-sdxl">AWS Bedrock SDXL</option>
                                        <option value="aws-bedrock-nova-canvas">AWS Bedrock Nova Canvas</option>
                                    </select>
                                    <small class="text-muted">Сервіс для генерації зображень</small>
                                </div>
                            </div>

                            <!-- Info Notice -->
                            <div class="alert alert-info">
                                <i class="bi bi-info-circle"></i>
                                <strong>Примітка:</strong> Технічні параметри генерації зображень (provider, розміри, steps, CFG scale) налаштовуються в <strong>Channel Config</strong> → Image Generation Settings. Це дозволяє кожному каналу мати свої технічні налаштування, використовуючи спільні AI інструкції з цього темплейту.
                            </div>
                        </div>

                                                <!-- Fields for Video Template -->
                        <div class="template-fields-group" id="video-fields" style="display: none;">
                            <!-- Transitions -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-film"></i> Transitions</h5>
                                </div>
                                <div id="transitions_container">
                                    <!-- Dynamic transition list -->
                                </div>
                                <button type="button" class="btn btn-sm btn-outline-primary mt-2" onclick="addTransition()">
                                    <i class="bi bi-plus-circle"></i> Add Transition
                                </button>
                                <small class="form-text text-muted d-block mt-1">Переходи між сценами</small>
                            </div>

                            <!-- Audio Mixing -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-volume-up"></i> Audio Mixing</h5>
                                    <p class="text-muted small mb-0">Рівні гучності (0.0 = silent, 1.0 = maximum)</p>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-4">
                                        <label class="form-label">Voice Volume</label>
                                        <input type="number" class="form-control" id="audio_voice_volume" min="0" max="1" step="0.1" value="1.0">
                                        <small class="text-muted">0.0 - 1.0</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Music Volume</label>
                                        <input type="number" class="form-control" id="audio_music_volume" min="0" max="1" step="0.1" value="0.3">
                                        <small class="text-muted">0.0 - 1.0</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">SFX Volume</label>
                                        <input type="number" class="form-control" id="audio_sfx_volume" min="0" max="1" step="0.1" value="0.5">
                                        <small class="text-muted">0.0 - 1.0</small>
                                    </div>
                                </div>
                                <div class="mt-3">
                                    <label class="form-check-label">
                                        <input type="checkbox" class="form-check-input" id="audio_normalize" checked>
                                        Normalize audio levels
                                    </label>
                                </div>
                            </div>

                            <!-- Export Settings -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-gear"></i> Export Settings</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label">Resolution</label>
                                        <select class="form-select" id="export_resolution">
                                            <option value="1920x1080" selected>1080p (1920x1080)</option>
                                            <option value="2560x1440">1440p (2560x1440)</option>
                                            <option value="3840x2160">4K (3840x2160)</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Frame Rate</label>
                                        <select class="form-select" id="export_fps">
                                            <option value="24">24 fps (cinematic)</option>
                                            <option value="30" selected>30 fps (standard)</option>
                                            <option value="60">60 fps (smooth)</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="row g-3 mt-2">
                                    <div class="col-md-6">
                                        <label class="form-label">Codec</label>
                                        <select class="form-select" id="export_codec">
                                            <option value="libx264" selected>H.264 (libx264) - Compatible</option>
                                            <option value="libx265">H.265 (libx265) - Smaller files</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Bitrate</label>
                                        <select class="form-select" id="export_bitrate">
                                            <option value="5M">5 Mbps (low quality)</option>
                                            <option value="8M" selected>8 Mbps (recommended)</option>
                                            <option value="12M">12 Mbps (high quality)</option>
                                            <option value="20M">20 Mbps (very high)</option>
                                        </select>
                                    </div>
                                </div>
                            </div>

                            <!-- Visual Effects -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-magic"></i> Visual Effects</h5>
                                </div>
                                <div class="mb-3">
                                    <label class="form-check-label">
                                        <input type="checkbox" class="form-check-input" id="visual_effects_enabled" checked>
                                        Enable visual effects
                                    </label>
                                </div>
                                <div id="visual_effects_options">
                                    <h6 class="mb-2">Ken Burns Effect (Zoom)</h6>
                                    <div class="row g-3">
                                        <div class="col-md-6">
                                            <label class="form-check-label">
                                                <input type="checkbox" class="form-check-input" id="ken_burns_enabled" checked>
                                                Enable Ken Burns zoom effect
                                            </label>
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label">Zoom Factor</label>
                                            <input type="number" class="form-control" id="ken_burns_zoom" min="1.0" max="1.5" step="0.1" value="1.1">
                                            <small class="text-muted">1.0 = no zoom, 1.5 = max zoom</small>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Thumbnail Intro -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-card-image"></i> Thumbnail Intro</h5>
                                    <p class="text-muted small mb-0">Показувати thumbnail на початку відео</p>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-check-label">
                                            <input type="checkbox" class="form-check-input" id="thumbnail_intro_enabled">
                                            Show thumbnail at video start
                                        </label>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Duration (seconds)</label>
                                        <input type="number" class="form-control" id="thumbnail_intro_duration" min="1" max="10" value="3">
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Fields for CTA Template -->
                        <div class="template-fields-group" id="cta-fields" style="display: none;">
                            <!-- CTA Type & Position -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-megaphone"></i> CTA Configuration</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-4">
                                        <label class="form-label">CTA Type</label>
                                        <select class="form-select" id="cta_type">
                                            <option value="subscribe" selected>Subscribe</option>
                                            <option value="like">Like</option>
                                            <option value="comment">Comment</option>
                                            <option value="share">Share</option>
                                            <option value="playlist">Add to Playlist</option>
                                            <option value="custom">Custom</option>
                                        </select>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Position</label>
                                        <select class="form-select" id="cta_position">
                                            <option value="start">Start (intro)</option>
                                            <option value="middle">Middle</option>
                                            <option value="end" selected>End (outro)</option>
                                            <option value="overlay">Overlay</option>
                                        </select>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Duration (sec)</label>
                                        <input type="number" class="form-control" id="cta_duration" min="1" max="30" value="5">
                                    </div>
                                </div>
                            </div>

                            <!-- Visual Layout -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-layout-text-window"></i> Visual Layout</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label">Background Type</label>
                                        <select class="form-select" id="cta_bg_type">
                                            <option value="gradient" selected>Gradient</option>
                                            <option value="solid">Solid Color</option>
                                            <option value="image">Image</option>
                                            <option value="video">Video Loop</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Text Alignment</label>
                                        <select class="form-select" id="cta_text_align">
                                            <option value="left">Left</option>
                                            <option value="center" selected>Center</option>
                                            <option value="right">Right</option>
                                        </select>
                                    </div>
                                    <div class="col-md-12">
                                        <label class="form-label">Background Colors (hex, comma-separated)</label>
                                        <input type="text" class="form-control" id="cta_bg_colors" value="#667eea, #764ba2" placeholder="#667eea, #764ba2">
                                        <small class="text-muted">For gradient: 2+ colors. For solid: 1 color</small>
                                    </div>
                                </div>
                            </div>

                            <!-- Content Elements -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-card-text"></i> Content Elements</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-12">
                                        <label class="form-label">Main Text</label>
                                        <input type="text" class="form-control" id="cta_main_text" value="👻 Subscribe for more chilling stories" placeholder="Subscribe for more...">
                                    </div>
                                    <div class="col-md-12">
                                        <label class="form-label">Secondary Text (optional)</label>
                                        <input type="text" class="form-control" id="cta_secondary_text" placeholder="New videos every week!">
                                    </div>
                                    <div class="col-md-12">
                                        <label style="display: flex; align-items: center; gap: 8px;">
                                            <input type="checkbox" class="form-check-input" id="cta_show_button" checked>
                                            <span>Show Button</span>
                                        </label>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Button Text</label>
                                        <input type="text" class="form-control" id="cta_button_text" value="SUBSCRIBE" placeholder="SUBSCRIBE">
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Button Color</label>
                                        <input type="text" class="form-control" id="cta_button_color" value="#ff0000" placeholder="#ff0000">
                                    </div>
                                </div>
                            </div>

                            <!-- Animation -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-lightning"></i> Animation</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label">Entrance Animation</label>
                                        <select class="form-select" id="cta_entrance_anim">
                                            <option value="fade" selected>Fade In</option>
                                            <option value="slide-up">Slide Up</option>
                                            <option value="slide-down">Slide Down</option>
                                            <option value="zoom">Zoom In</option>
                                            <option value="none">None</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Exit Animation</label>
                                        <select class="form-select" id="cta_exit_anim">
                                            <option value="fade" selected>Fade Out</option>
                                            <option value="slide-up">Slide Up</option>
                                            <option value="slide-down">Slide Down</option>
                                            <option value="zoom">Zoom Out</option>
                                            <option value="none">None</option>
                                        </select>
                                    </div>
                                </div>
                            </div>

                            <!-- Voice-over -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-mic"></i> Voice-over (optional)</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-12">
                                        <label style="display: flex; align-items: center; gap: 8px;">
                                            <input type="checkbox" class="form-check-input" id="cta_enable_voiceover">
                                            <span>Enable voice-over for CTA</span>
                                        </label>
                                    </div>
                                    <div class="col-md-12">
                                        <label class="form-label">Voice-over Text</label>
                                        <textarea class="form-control" id="cta_voiceover_text" rows="2" placeholder="If you enjoyed this story, don't forget to subscribe for more chilling tales!"></textarea>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Fields for TTS Template -->
                        <div class="template-fields-group" id="tts-fields" style="display: none;">
                            <!-- TTS Service & Engine -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-cloud"></i> TTS Service & Engine</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label">TTS Service</label>
                                        <select class="form-select" id="tts_service">
                                            <option value="aws_polly_neural" selected>AWS Polly Neural ($16/1M chars) ⭐</option>
                                            <option value="aws_polly_standard">AWS Polly Standard ($4/1M chars)</option>
                                            <option value="elevenlabs">ElevenLabs (Premium)</option>
                                            <option value="google_tts">Google Cloud TTS</option>
                                        </select>
                                        <small class="text-muted">Neural має кращу якість і більше природний голос</small>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Voice Profile (AWS Neural)</label>
                                        <select class="form-select" id="tts_voice_profile">
                                            <optgroup label="🎙️ Жіночі голоси">
                                                <option value="joanna_female">Joanna - Жіночий (чіткий, професійний)</option>
                                                <option value="salli_female">Salli - Жіночий (теплий, дружній)</option>
                                                <option value="kimberly_female">Kimberly - Жіночий (енергійний, молодий)</option>
                                                <option value="kendra_female">Kendra - Жіночий (впевнений, професійний)</option>
                                                <option value="ivy_female">Ivy - Жіночий (молодий, дружній)</option>
                                                <option value="ruth_female">Ruth - Жіночий (авторитетний)</option>
                                                <option value="danielle_female">Danielle - Жіночий (м'який, приємний)</option>
                                            </optgroup>
                                            <optgroup label="🎤 Чоловічі голоси">
                                                <option value="matthew_male" selected>Matthew - Чоловічий (авторитетний, глибокий)</option>
                                                <option value="joey_male">Joey - Чоловічий (нейтральний, чіткий)</option>
                                                <option value="justin_male">Justin - Чоловічий (молодий, енергійний)</option>
                                                <option value="gregory_male">Gregory - Чоловічий (британський, авторитетний)</option>
                                                <option value="kevin_male">Kevin - Чоловічий (молодий, дружній)</option>
                                                <option value="stephen_male">Stephen - Чоловічий (професійний)</option>
                                            </optgroup>
                                        </select>
                                        <small class="text-muted">Виберіть голос AWS Polly Neural для озвучки</small>
                                    </div>
                                </div>
                            </div>

                            <!-- SSML Prosody Settings -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-sliders"></i> SSML Prosody (Швидкість, Тон, Гучність)</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-4">
                                        <label class="form-label">Rate (Швидкість) <i class="bi bi-info-circle" title="Швидкість мовлення"></i></label>
                                        <select class="form-select" id="tts_prosody_rate">
                                            <option value="x-slow">Extra Slow (x-slow)</option>
                                            <option value="slow">Slow</option>
                                            <option value="medium" selected>Medium (default)</option>
                                            <option value="fast">Fast</option>
                                            <option value="x-fast">Extra Fast (x-fast)</option>
                                            <option value="80%">80% (slower)</option>
                                            <option value="100%">100% (normal)</option>
                                            <option value="120%">120% (faster)</option>
                                            <option value="150%">150% (much faster)</option>
                                        </select>
                                        <small class="text-muted">Named або % значення</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Pitch (Тон) <i class="bi bi-info-circle" title="Висота голосу"></i></label>
                                        <select class="form-select" id="tts_prosody_pitch">
                                            <option value="x-low">Extra Low (x-low)</option>
                                            <option value="low">Low</option>
                                            <option value="medium" selected>Medium (default)</option>
                                            <option value="high">High</option>
                                            <option value="x-high">Extra High (x-high)</option>
                                            <option value="-10%">-10% (lower)</option>
                                            <option value="0%">0% (normal)</option>
                                            <option value="+5%">+5% (higher)</option>
                                            <option value="+10%">+10% (much higher)</option>
                                        </select>
                                        <small class="text-muted">Named або % значення</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Volume (Гучність) <i class="bi bi-info-circle" title="Рівень гучності"></i></label>
                                        <select class="form-select" id="tts_prosody_volume">
                                            <option value="silent">Silent</option>
                                            <option value="x-soft">Extra Soft (x-soft)</option>
                                            <option value="soft">Soft</option>
                                            <option value="medium" selected>Medium (default)</option>
                                            <option value="loud">Loud</option>
                                            <option value="x-loud">Extra Loud (x-loud)</option>
                                            <option value="+0dB">+0dB (normal)</option>
                                            <option value="+3dB">+3dB (louder)</option>
                                            <option value="+6dB">+6dB (much louder)</option>
                                        </select>
                                        <small class="text-muted">Named або dB значення</small>
                                    </div>
                                </div>
                            </div>

                            <!-- SSML Pauses & Breaks -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-pause"></i> SSML Pauses (break time)</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-4">
                                        <label class="form-label">After Sentence (речення)</label>
                                        <input type="text" class="form-control" id="tts_break_sentence" value="300ms" placeholder="300ms">
                                        <small class="text-muted">Формат: 500ms або 1s</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">After Paragraph (параграф)</label>
                                        <input type="text" class="form-control" id="tts_break_paragraph" value="600ms" placeholder="600ms">
                                        <small class="text-muted">Формат: 500ms або 1s</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Between Scenes (сцени)</label>
                                        <input type="text" class="form-control" id="tts_break_scene" value="1000ms" placeholder="1000ms">
                                        <small class="text-muted">Формат: 1000ms або 1s</small>
                                    </div>
                                </div>
                                <div class="row g-3 mt-2">
                                    <div class="col-md-6">
                                        <label class="form-label">Dramatic Pause (драматична пауза)</label>
                                        <input type="text" class="form-control" id="tts_break_dramatic" value="1500ms" placeholder="1500ms">
                                        <small class="text-muted">Для особливих моментів в наративі</small>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Default Break Strength</label>
                                        <select class="form-select" id="tts_break_strength">
                                            <option value="none">None</option>
                                            <option value="x-weak">Extra Weak (x-weak)</option>
                                            <option value="weak">Weak</option>
                                            <option value="medium" selected>Medium</option>
                                            <option value="strong">Strong</option>
                                            <option value="x-strong">Extra Strong (x-strong)</option>
                                        </select>
                                        <small class="text-muted">Strength для break без time</small>
                                    </div>
                                </div>
                            </div>

                            <!-- SSML Emphasis -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-chat-quote"></i> SSML Emphasis (Наголос)</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label">Default Emphasis Level</label>
                                        <select class="form-select" id="tts_emphasis_level">
                                            <option value="reduced">Reduced (знижений)</option>
                                            <option value="moderate" selected>Moderate (помірний)</option>
                                            <option value="strong">Strong (сильний)</option>
                                        </select>
                                        <small class="text-muted">Використовується для важливих слів/фраз</small>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Narrative Pace</label>
                                        <select class="form-select" id="tts_narrative_pace">
                                            <option value="slow">Slow (повільний)</option>
                                            <option value="medium" selected>Medium (середній)</option>
                                            <option value="fast">Fast (швидкий)</option>
                                        </select>
                                        <small class="text-muted">Загальний темп наративу</small>
                                    </div>
                                </div>
                            </div>

                            <!-- SSML Validation & Options -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-check2-circle"></i> SSML Валідація і Додаткові Опції</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" id="tts_auto_fix_ssml" checked>
                                            <label class="form-check-label" for="tts_auto_fix_ssml">
                                                Auto-fix SSML (рекомендовано)
                                            </label>
                                            <small class="d-block text-muted">Автоматично виправляти лапки, додавати &lt;speak&gt;, додавати ms до break</small>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" id="tts_validate_strict">
                                            <label class="form-check-label" for="tts_validate_strict">
                                                Strict Validation
                                            </label>
                                            <small class="d-block text-muted">Повертати помилку якщо SSML невалідний (інакше - тільки warning)</small>
                                        </div>
                                    </div>
                                </div>
                                <div class="row g-3 mt-2">
                                    <div class="col-12">
                                        <label class="form-label">Mood Variants (for TTS) <i class="bi bi-info-circle" title="Варіанти настрою для озвучки"></i></label>
                                        <input type="text" class="form-control" id="tts_mood_variants" placeholder="Mysterious, suspenseful, calm">
                                        <small class="text-muted">Комою через кому: настрої які Lambda може використати для варіацій (якщо TTS підтримує)</small>
                                    </div>
                                </div>
                            </div>

                            <!-- Audio Output Settings -->
                            <div class="section-card">
                                <div class="section-header">
                                    <h5 class="section-title"><i class="bi bi-music-note-beamed"></i> Audio Output Quality</h5>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-4">
                                        <label class="form-label">Output Format</label>
                                        <select class="form-select" id="tts_output_format">
                                            <option value="mp3" selected>MP3 (compressed, smaller)</option>
                                            <option value="ogg_vorbis">OGG Vorbis (open source)</option>
                                            <option value="pcm">PCM (uncompressed, large)</option>
                                        </select>
                                        <small class="text-muted">AWS Polly підтримує ці формати</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Sample Rate</label>
                                        <select class="form-select" id="tts_sample_rate">
                                            <option value="8000">8 kHz (telephone)</option>
                                            <option value="16000">16 kHz (standard)</option>
                                            <option value="22050">22.05 kHz</option>
                                            <option value="24000" selected>24 kHz (high quality) ⭐</option>
                                        </select>
                                        <small class="text-muted">Вища частота = краща якість</small>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label">Language Code</label>
                                        <select class="form-select" id="tts_language_code">
                                            <option value="en-US" selected>English (US)</option>
                                            <option value="en-GB">English (UK)</option>
                                            <option value="en-AU">English (Australia)</option>
                                            <option value="en-IN">English (India)</option>
                                        </select>
                                        <small class="text-muted">Мова і акцент голосу</small>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- SFX Template Fields -->
<div class="template-fields-group" id="sfx-fields" style="display: none;">
    <!-- AI Configuration for SFX -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-robot"></i> AI Configuration</h5>
        </div>
        <div class="row g-3">
            <div class="col-md-4">
                <label class="form-label">Model</label>
                <select class="form-select" id="sfx_model">
                    <option value="gpt-4o">GPT-4o</option>
                    <option value="gpt-4o-mini" selected>GPT-4o Mini</option>
                </select>
            </div>
            <div class="col-md-4">
                <label class="form-label">Temperature</label>
                <input type="number" class="form-control" id="sfx_temperature" step="0.1" min="0" max="2" value="0.7">
            </div>
            <div class="col-md-4">
                <label class="form-label">Max Tokens</label>
                <input type="number" class="form-control" id="sfx_max_tokens" step="100" value="3000">
            </div>
        </div>
        <div class="row g-3 mt-2">
            <div class="col-12">
                <label class="form-label">Role Definition</label>
                <textarea class="form-control" id="sfx_role_definition" rows="3" placeholder="You are SFX Agent..."></textarea>
            </div>
        </div>
    </div>

    <!-- Core Rules for SFX -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-list-check"></i> Core Rules</h5>
        </div>
        <div id="sfxCoreRulesContainer"></div>
        <button type="button" class="btn btn-outline-primary btn-sm mt-2" onclick="addSfxCoreRule()">
            <i class="bi bi-plus-circle"></i> Add Rule
        </button>
    </div>

    <!-- Timing Rules -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-clock"></i> Timing Rules</h5>
        </div>
        <div class="row g-3">
            <div class="col-md-6">
                <label class="form-label">Max SFX per Scene</label>
                <input type="number" class="form-control" id="sfx_max_per_scene" value="3">
            </div>
            <div class="col-md-6">
                <label class="form-label">Average Words per Minute</label>
                <input type="number" class="form-control" id="sfx_words_per_minute" value="150">
            </div>
        </div>
    </div>
</div>

<!-- Thumbnail Template Fields -->
<div class="template-fields-group" id="thumbnail-fields" style="display: none;">
    <!-- AI Configuration for Thumbnail -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-robot"></i> AI Configuration</h5>
        </div>
        <div class="row g-3">
            <div class="col-md-4">
                <label class="form-label">Model</label>
                <select class="form-select" id="thumbnail_model">
                    <option value="gpt-4o">GPT-4o</option>
                    <option value="gpt-4o-mini" selected>GPT-4o Mini</option>
                </select>
            </div>
            <div class="col-md-4">
                <label class="form-label">Temperature</label>
                <input type="number" class="form-control" id="thumbnail_temperature" step="0.1" min="0" max="2" value="0.8">
            </div>
            <div class="col-md-4">
                <label class="form-label">Max Tokens</label>
                <input type="number" class="form-control" id="thumbnail_max_tokens" step="100" value="2000">
            </div>
        </div>
        <div class="row g-3 mt-2">
            <div class="col-12">
                <label class="form-label">Role Definition</label>
                <textarea class="form-control" id="thumbnail_role_definition" rows="3" placeholder="You are Thumbnail Designer..."></textarea>
            </div>
        </div>
    </div>

    <!-- Core Rules for Thumbnail -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-list-check"></i> Core Rules</h5>
        </div>
        <div id="thumbnailCoreRulesContainer"></div>
        <button type="button" class="btn btn-outline-primary btn-sm mt-2" onclick="addThumbnailCoreRule()">
            <i class="bi bi-plus-circle"></i> Add Rule
        </button>
    </div>

    <!-- Thumbnail Settings -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-image"></i> Thumbnail Settings</h5>
        </div>
        <div class="row g-3">
            <div class="col-md-4">
                <label class="form-label">Aspect Ratio</label>
                <select class="form-select" id="thumbnail_aspect_ratio">
                    <option value="16:9" selected>16:9 (YouTube Standard)</option>
                    <option value="4:3">4:3</option>
                    <option value="1:1">1:1 (Square)</option>
                </select>
            </div>
            <div class="col-md-4">
                <label class="form-label">Resolution</label>
                <select class="form-select" id="thumbnail_resolution">
                    <option value="1280x720" selected>1280x720 (HD)</option>
                    <option value="1920x1080">1920x1080 (Full HD)</option>
                </select>
            </div>
            <div class="col-md-4">
                <label class="form-label">Text Overlay</label>
                <select class="form-select" id="thumbnail_text_overlay">
                    <option value="true" selected>Enabled</option>
                    <option value="false">Disabled</option>
                </select>
            </div>
        </div>
        <div class="row g-3 mt-2">
            <div class="col-md-6">
                <label class="form-label">Max Text Words</label>
                <input type="number" class="form-control" id="thumbnail_max_text_words" value="5">
                <small class="text-muted">Maximum words for main text overlay</small>
            </div>
            <div class="col-md-6">
                <label class="form-label">Font Size Range</label>
                <input type="text" class="form-control" id="thumbnail_font_size_range" value="80-120">
                <small class="text-muted">Min-Max font size in pixels</small>
            </div>
        </div>
    </div>

    <!-- Typography Rules -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-fonts"></i> Typography Rules</h5>
        </div>
        <div class="row g-3">
            <div class="col-12">
                <label class="form-label">Recommended Fonts</label>
                <input type="text" class="form-control" id="thumbnail_recommended_fonts" value="Impact, Bebas Neue, Anton Bold">
                <small class="text-muted">Comma-separated font names</small>
            </div>
        </div>
        <div class="row g-3 mt-2">
            <div class="col-md-6">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="thumbnail_stroke_required" checked>
                    <label class="form-check-label" for="thumbnail_stroke_required">
                        Text Stroke Required
                    </label>
                    <small class="d-block text-muted">Add outline to text for better visibility</small>
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="thumbnail_shadow_required" checked>
                    <label class="form-check-label" for="thumbnail_shadow_required">
                        Text Shadow Required
                    </label>
                    <small class="d-block text-muted">Add shadow for depth</small>
                </div>
            </div>
        </div>
    </div>

    <!-- Composition Rules -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-grid-3x3"></i> Composition Rules</h5>
        </div>
        <div class="row g-3">
            <div class="col-md-6">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="thumbnail_rule_of_thirds" checked>
                    <label class="form-check-label" for="thumbnail_rule_of_thirds">
                        Use Rule of Thirds
                    </label>
                </div>
            </div>
            <div class="col-md-6">
                <label class="form-label">Focal Points</label>
                <input type="text" class="form-control" id="thumbnail_focal_points" value="center_dominant, left_third_right_text">
                <small class="text-muted">Comma-separated composition styles</small>
            </div>
        </div>
    </div>
</div>

<!-- Description Template Fields -->
<div class="template-fields-group" id="description-fields" style="display: none;">
    <!-- AI Configuration for Description -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-robot"></i> AI Configuration</h5>
        </div>
        <div class="row g-3">
            <div class="col-md-4">
                <label class="form-label">Model</label>
                <select class="form-select" id="description_model">
                    <option value="gpt-4o">GPT-4o</option>
                    <option value="gpt-4o-mini" selected>GPT-4o Mini</option>
                </select>
            </div>
            <div class="col-md-4">
                <label class="form-label">Temperature</label>
                <input type="number" class="form-control" id="description_temperature" step="0.1" min="0" max="2" value="0.7">
            </div>
            <div class="col-md-4">
                <label class="form-label">Max Tokens</label>
                <input type="number" class="form-control" id="description_max_tokens" step="100" value="2000">
            </div>
        </div>
        <div class="row g-3 mt-2">
            <div class="col-12">
                <label class="form-label">Role Definition</label>
                <textarea class="form-control" id="description_role_definition" rows="3" placeholder="You are SEO Expert and Video Description Writer..."></textarea>
            </div>
        </div>
    </div>

    <!-- Core Rules for Description -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-list-check"></i> Core Rules</h5>
        </div>
        <div id="descriptionCoreRulesContainer"></div>
        <button type="button" class="btn btn-outline-primary btn-sm mt-2" onclick="addDescriptionCoreRule()">
            <i class="bi bi-plus-circle"></i> Add Rule
        </button>
    </div>

    <!-- SEO Keywords -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-search"></i> SEO Keywords</h5>
        </div>
        <div class="row g-3">
            <div class="col-12">
                <label class="form-label">Target Keywords</label>
                <textarea class="form-control" id="description_seo_keywords" rows="3" placeholder="mythology, ancient history, epic stories"></textarea>
                <small class="text-muted">Comma-separated keywords for SEO optimization</small>
            </div>
        </div>
    </div>

    <!-- Description Structure -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-file-text"></i> Description Structure</h5>
        </div>
        <div class="row g-3">
            <div class="col-md-6">
                <label class="form-label">Hook Max Length</label>
                <input type="number" class="form-control" id="description_hook_max_length" value="150">
                <small class="text-muted">Characters for first 2 lines (visible without "Show More")</small>
            </div>
            <div class="col-md-6">
                <label class="form-label">Total Max Length</label>
                <input type="number" class="form-control" id="description_total_max_length" value="5000">
                <small class="text-muted">Total description length limit</small>
            </div>
        </div>
        <div class="row g-3 mt-2">
            <div class="col-md-6">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="description_include_timestamps" checked>
                    <label class="form-check-label" for="description_include_timestamps">
                        Include Timestamps
                    </label>
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="description_include_hashtags" checked>
                    <label class="form-check-label" for="description_include_hashtags">
                        Include Hashtags
                    </label>
                </div>
            </div>
        </div>
        <div class="row g-3 mt-2">
            <div class="col-12">
                <label class="form-label">Max Hashtags</label>
                <input type="number" class="form-control" id="description_max_hashtags" value="5">
                <small class="text-muted">Maximum number of hashtags to include</small>
            </div>
        </div>
    </div>

    <!-- Social Links -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title"><i class="bi bi-share"></i> Social Links Template</h5>
        </div>
        <div class="row g-3">
            <div class="col-12">
                <label class="form-label">Social Links Format</label>
                <textarea class="form-control" id="description_social_links" rows="4" placeholder="Follow us on:
- Twitter: @channel
- Instagram: @channel
- Discord: discord.gg/channel"></textarea>
                <small class="text-muted">Template for social media links section</small>
            </div>
        </div>
    </div>
</div>


                        <!-- Hook Rules -->
                        <div class="section-card" id="hookRulesSection">
                            <div class="section-header">
                                <h5 class="section-title"><i class="bi bi-megaphone"></i> Hook Rules</h5>
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" id="hook_enabled" checked>
                                    <label class="form-check-label" for="hook_enabled">Enable Hooks</label>
                                </div>
                            </div>
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label class="form-label">Style</label>
                                    <input type="text" class="form-control" id="hook_style" placeholder="Question-based or shock">
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Length</label>
                                    <input type="text" class="form-control" id="hook_length" placeholder="50-120 characters">
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Placement</label>
                                    <input type="text" class="form-control" id="hook_placement" placeholder="first_15_seconds">
                                </div>
                            </div>
                        </div>

                        <!-- SSML Settings (for Narrative Templates only) -->
                        <div class="section-card" id="ssmlSettingsSection" style="display: none;">
                            <div class="section-header">
                                <h5 class="section-title"><i class="bi bi-soundwave"></i> SSML Settings</h5>
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" id="use_ssml" checked onchange="toggleSSMLOptions()">
                                    <label class="form-check-label" for="use_ssml">Generate SSML Markup</label>
                                </div>
                            </div>
                            <div id="ssmlOptionsContent">
                                <div class="row g-3 mb-3">
                                    <div class="col-md-6">
                                        <label class="form-label">SSML Style</label>
                                        <select class="form-select" id="ssml_style">
                                            <option value="emotional">Emotional (pauses + emphasis)</option>
                                            <option value="neutral">Neutral (minimal markup)</option>
                                            <option value="dramatic">Dramatic (maximum effect)</option>
                                            <option value="conversational">Conversational</option>
                                            <option value="documentary">Documentary</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Default Pace</label>
                                        <select class="form-select" id="ssml_default_pace">
                                            <option value="slow">Slow</option>
                                            <option value="medium" selected>Medium</option>
                                            <option value="fast">Fast</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">SSML Guidelines (for AI)</label>
                                    <textarea class="form-control" id="ssml_guidelines" rows="4" placeholder="Add emotional pauses after important moments.
Use emphasis on key words and character names.
Vary prosody rate for dramatic effect.">Add emotional pauses after important moments.
Use &lt;emphasis level='strong'&gt; on key words.
Slow down during tense moments.</textarea>
                                    <small class="text-muted">Instructions for OpenAI on how to generate SSML markup</small>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label">Pause After Scene Intro</label>
                                        <input type="text" class="form-control" id="pause_after_intro" value="800ms">
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label">Pause After Dramatic Moment</label>
                                        <input type="text" class="form-control" id="pause_dramatic" value="1500ms">
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Save Button (Bottom) -->
                        <div class="d-flex justify-content-end gap-2 mt-4 editor-footer-buttons">
                            <button type="button" class="btn btn-outline-secondary" onclick="backToLibrary()">Cancel</button>
                            <button type="button" id="deleteBtn" class="btn btn-danger" onclick="deleteTemplate()" style="display: none;">
                                <i class="bi bi-trash"></i> Delete Template
                            </button>
                            <button type="button" class="btn btn-success btn-lg" onclick="saveTemplate()">
                                <i class="bi bi-save"></i> Save Template
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Preview View -->
                <div class="tab-pane fade" id="preview" role="tabpanel">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h3>Preview Compiled Prompt</h3>
                        <div>
                            <select class="form-select d-inline-block w-auto me-2" id="previewChannelSelect">
                                <option value="">Select Channel...</option>
                            </select>
                            <button class="btn btn-primary" onclick="generatePreview()">
                                <i class="bi bi-arrow-clockwise"></i> Generate Preview
                            </button>
                        </div>
                    </div>

                    <div id="previewContainer">
                        <div class="alert alert-info">
                            <i class="bi bi-info-circle"></i> Select a channel and click "Generate Preview" to see the compiled prompt
                        </div>
                    </div>
                </div>

                <!-- Version History View -->
                <div class="tab-pane fade" id="history" role="tabpanel">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h3>Version History</h3>
                    </div>

                    <div id="versionHistoryContainer">
                        <!-- Version history will be loaded here -->
                        <div class="text-center py-5">
                            <div class="loading-spinner"></div>
                            <p class="text-muted mt-3">Loading version history...</p>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <!-- Template Editor Modal -->
    <div class="modal fade" id="editorModal" tabindex="-1" aria-labelledby="editorModalTitle" aria-hidden="true" data-bs-backdrop="static">
        <div class="modal-dialog modal-xl modal-dialog-scrollable modal-fullscreen-lg-down">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="editorModalTitle">Edit Template</h5>
                    <button type="button" class="btn-close" onclick="closeEditorModal()" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="editorModalBody">
                    <!-- Form from editor tab will be moved here via JavaScript -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" onclick="closeEditorModal()">
                        <i class="bi bi-x"></i> Cancel
                    </button>
                    <button type="button" id="deleteBtnModal" class="btn btn-danger" onclick="deleteTemplate()" style="display: none;">
                        <i class="bi bi-trash"></i> Delete Template
                    </button>
                    <button type="button" class="btn btn-success btn-lg" onclick="saveTemplate()">
                        <i class="bi bi-save"></i> Save Template
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Toast Container -->
    <div class="toast-container"></div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Global state
        let currentTemplate = null;
        let allTemplates = [];
        let allChannels = [];

        const API_BASE = 'https://djpb4ue6wv2ohfjey32lfnhcre0zppqd.lambda-url.eu-central-1.on.aws';
        const EC2_SD35_API_URL = 'https://smhnyrluhutva5epkljlbd6kxe0cvykf.lambda-url.eu-central-1.on.aws/';  // ec2-sd35-control Lambda Function URL
        let ec2SD35StatusInterval = null;
        let ec2SD35UserAction = null; // Track user action: 'starting', 'stopping', null

        // VERSION: 2.3 - Fixed state tracking for start/stop (2025-11-09)
        console.log('🚀 Prompts Editor loaded - Version 2.3');
        console.log('📡 EC2 SD35 Control API URL:', EC2_SD35_API_URL);

        // Initialize
        document.addEventListener('DOMContentLoaded', () => {
            console.log('📄 DOM Content Loaded');
            loadTemplates();
            loadChannels();
            refreshEC2SD35Status();
        });

        // ====================================================================
        // LIBRARY FUNCTIONS
        // ====================================================================

        async function loadTemplates() {
            // Load current template type (default to theme)
            loadTemplatesByType(currentTemplateType || 'theme');
        }

        // Current template type
        let currentTemplateType = 'theme';
        let isCreatingNew = false;  // Track if we're creating a new template

        function switchTemplateType(type) {
            console.log('Switching to template type:', type);
            currentTemplateType = type;

            // Hide all template type panes
            document.querySelectorAll('.template-type-pane').forEach(pane => {
                pane.classList.remove('active');
                pane.style.display = 'none';
            });

            // Remove active class from all tabs
            document.querySelectorAll('#templateTypeTabs .nav-link').forEach(tab => {
                tab.classList.remove('active');
            });

            // Show selected template type pane
            const selectedPane = document.getElementById(`${type}-templates-pane`);
            if (selectedPane) {
                selectedPane.classList.add('active');
                selectedPane.style.display = 'block';
            }

            // Add active class to selected tab
            const selectedTab = document.getElementById(`${type}-templates-tab`);
            if (selectedTab) {
                selectedTab.classList.add('active');
            }

            // Load templates for this type
            loadTemplatesByType(type);
        }

        async function loadTemplatesByType(type) {
            const gridId = `${type}TemplatesGrid`;
            const grid = document.getElementById(gridId);

            if (!grid) {
                console.error(`Grid not found: ${gridId}`);
                return;
            }

            // Show loading
            grid.innerHTML = `
                <div class="col-12 text-center py-5">
                    <div class="loading-spinner"></div>
                    <p class="text-muted mt-3">Loading ${type} templates...</p>
                </div>
            `;

            try {
                const response = await fetch(`${API_BASE}/?type=${type}`);
                const data = await response.json();

                if (data.success) {
                    // Templates are already filtered by type in the backend
                    const templates = data.data.templates;

                    if (templates.length === 0) {
                        // Show empty state
                        const typeNames = {
                            'theme': 'theme',
                            'narrative': 'narrative',
                            'image': 'image generation',
                            'video': 'video editing',
                            'cta': 'CTA',
                            'tts': 'TTS/voice',
                            'sfx': 'SFX',
                            'thumbnail': 'thumbnail',
                            'description': 'description'
                        };
                        const icons = {
                            'theme': 'lightbulb',
                            'narrative': 'file-text',
                            'image': 'stars',
                            'video': 'film',
                            'cta': 'megaphone',
                            'tts': 'mic',
                            'sfx': 'soundwave',
                            'thumbnail': 'card-image',
                            'description': 'card-text'
                        };
                        grid.innerHTML = `
                            <div class="col-12 text-center py-5">
                                <i class="bi bi-${icons[type]}" style="font-size: 3rem; color: rgba(102, 126, 234, 0.3);"></i>
                                <p class="text-muted mt-3">No ${typeNames[type]} templates yet</p>
                                <button class="btn btn-outline-primary mt-2" onclick="createNewTemplate('${type}')">
                                    Create First Template
                                </button>
                            </div>
                        `;
                    } else {
                        // Render templates
                        renderTemplateGridByType(templates, grid);
                    }
                } else {
                    throw new Error(data.error || 'Failed to load templates');
                }
            } catch (error) {
                console.error('Error loading templates:', error);
                grid.innerHTML = `
                    <div class="col-12">
                        <div class="alert alert-danger">
                            <i class="bi bi-exclamation-triangle"></i>
                            Failed to load templates: ${error.message}
                        </div>
                    </div>
                `;
            }
        }

        function renderTemplateGridByType(templates, grid) {
            grid.innerHTML = templates.map(template => `
                <div class="col-md-6 col-lg-4">
                    <div class="template-card active">
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <div class="d-flex gap-2">
                                ${template.is_default ? '<span class="template-badge" style="background: rgba(16, 185, 129, 0.2); color: #10b981; border: 1px solid rgba(16, 185, 129, 0.3);"><i class="bi bi-shield-check"></i> Default</span>' : ''}
                            </div>
                            <span class="text-muted">v${template.version || 1}</span>
                        </div>
                        <h5 class="mb-2">${template.template_name}</h5>
                        <p class="text-muted small mb-3">${template.description || 'No description'}</p>
                        <div class="d-flex justify-content-between text-muted small mb-3">
                            <span><i class="bi bi-tag"></i> ${template.template_type}</span>
                            <span><i class="bi bi-clock"></i> ${new Date(template.updated_at || Date.now()).toLocaleDateString()}</span>
                        </div>
                        <div class="d-flex gap-2">
                            <button class="btn btn-sm btn-primary flex-fill" onclick="editTemplate('${template.template_id}')">
                                <i class="bi bi-pencil"></i> Edit
                            </button>
                            <button class="btn btn-sm btn-outline-secondary" onclick="event.stopPropagation(); cloneTemplate('${template.template_id}')">
                                <i class="bi bi-files"></i> Clone
                            </button>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function createNewTemplate(type) {
            console.log('Creating new template of type:', type);

            isCreatingNew = true;
            currentTemplateType = type;

            // Create empty template with default values based on type
            const defaultTemplates = {
                'theme': {
                    template_type: 'theme',
                    template_name: 'New Theme Template',
                    agent_type: 'theme_agent',
                    description: '',
                    model: 'gpt-4o',
                    temperature: 0.8,
                    max_tokens: 4000,
                    is_active: true,
                    sections: {
                        role_definition: 'You are a creative theme generator for YouTube videos.',
                        core_rules: ['Generate engaging themes', 'Focus on trending topics', 'Be original'],
                        hook_rules: { enabled: false, style: '', length: '', placement: '' }
                    }
                },
                'narrative': {
                    template_type: 'narrative',
                    template_name: 'New Narrative Template',
                    agent_type: 'narrative_architect',
                    description: '',
                    model: 'gpt-4o',
                    temperature: 0.8,
                    max_tokens: 8000,
                    is_active: true,
                    sections: {
                        role_definition: 'You are a narrative architect creating compelling video scripts.',
                        core_rules: ['Structure scenes logically', 'Maintain engagement', 'Build suspense'],
                        hook_rules: {
                            enabled: true,
                            style: 'Question-based or shock',
                            length: '50-120 characters',
                            placement: 'first_15_seconds'
                        },
                        ssml_settings: {
                            enabled: true,
                            style: 'emotional',
                            default_pace: 'medium',
                            guidelines: 'Add emotional pauses after important moments.\nUse <emphasis level=\'strong\'> on key words.\nSlow down during tense moments.',
                            pause_after_intro: '800ms',
                            pause_dramatic: '1500ms'
                        }
                    }
                },
                'image': {
                    template_type: 'image',
                    template_name: 'New Image Generation Template',
                    description: 'Image generation settings for FLUX/SDXL',
                    is_active: true,
                    image_settings: {
                        provider: 'ec2-sd35',
                        service: 'sd35-medium',
                        width: 1024,
                        height: 576,
                        quality: 'standard',
                        steps: 4,
                        cfg_scale: 0,
                        aspect_ratio: '16:9'
                    }
                },
                'video': {
                    template_type: 'video',
                    template_name: 'New Video Template',
                    description: '',
                    is_active: true,
                    genre: 'General',
                    editing_params: {
                        transition_style: 'smooth',
                        transition_duration: 0.5,
                        scene_duration_min: 5,
                        scene_duration_max: 15,
                        pacing: 'medium',
                        effects: ['fade', 'dissolve']
                    }
                },
                'cta': {
                    template_type: 'cta',
                    template_name: 'New CTA Template',
                    description: '',
                    is_active: true,
                    genre: 'General',
                    cta_type: 'subscribe',
                    position: 'end',
                    duration: 5,
                    layout: {
                        background: { type: 'gradient', colors: ['#667eea', '#764ba2'] },
                        text_alignment: 'center'
                    },
                    elements: [
                        { type: 'text', content: 'Thanks for watching!', style: { font_size: 'large', color: '#ffffff' } },
                        { type: 'button', content: 'SUBSCRIBE', action: 'subscribe', style: { background: '#ff0000', color: '#ffffff' } }
                    ],
                    animation: {
                        entrance: 'fade-in',
                        entrance_duration: 0.5,
                        exit: 'fade-out',
                        exit_duration: 0.5
                    }
                },
                'tts': {
                    template_type: 'tts',
                    template_name: 'New TTS/Voice Template',
                    description: 'AWS Polly TTS with SSML settings',
                    is_active: true,
                    tts_settings: {
                        // TTS Service & Engine
                        tts_service: 'aws_polly_neural',
                        tts_voice_profile: 'neutral_male',

                        // SSML Prosody
                        prosody_rate: 'medium',
                        prosody_pitch: 'medium',
                        prosody_volume: 'medium',

                        // SSML Pauses & Breaks
                        break_sentence: '300ms',
                        break_paragraph: '600ms',
                        break_scene: '1000ms',
                        break_dramatic: '1500ms',
                        break_strength: 'medium',

                        // SSML Emphasis
                        emphasis_level: 'moderate',
                        narrative_pace: 'medium',

                        // SSML Validation
                        auto_fix_ssml: true,
                        validate_strict: false,
                        tts_mood_variants: '',

                        // Audio Output
                        output_format: 'mp3',
                        sample_rate: '24000',
                        language_code: 'en-US'
                    }
                }
            };

            currentTemplate = defaultTemplates[type] || defaultTemplates['theme'];

            // Populate form with default values
            populateEditor(currentTemplate);

            // Update UI to show "Create" mode
            document.getElementById('editorTitle').textContent = `Create New ${type.charAt(0).toUpperCase() + type.slice(1)} Template`;

            // Hide delete buttons for new templates
            document.getElementById('deleteBtn').style.display = 'none';
            const deleteBtnModal = document.getElementById('deleteBtnModal');
            if (deleteBtnModal) deleteBtnModal.style.display = 'none';

            // Open modal editor
            openEditorModal();

            showToast(`Creating new ${type} template. Fill in the details and click Save.`, 'info');
        }

        // ====================================================================
        // EDITOR FUNCTIONS
        // ====================================================================

        async function editTemplate(templateId) {
            try {
                const response = await fetch(`${API_BASE}/template/${templateId}?type=${currentTemplateType}`);
                const data = await response.json();

                if (data.success) {
                    currentTemplate = data.data.template;
                    isCreatingNew = false;  // Reset creation mode when editing existing template

                    // Populate form
                    populateEditor(currentTemplate);

                    // Open modal editor
                    openEditorModal();
                } else {
                    showToast('Error loading template: ' + data.error, 'danger');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('Failed to load template: ' + error.message, 'danger');
            }
        }

        function openEditorModal() {
            // Move form from editor tab to modal
            const form = document.getElementById('templateForm');
            const modalBody = document.getElementById('editorModalBody');

            if (form && modalBody) {
                modalBody.innerHTML = '';
                modalBody.appendChild(form);
            }

            // Update modal title (use editorTitle which is already set by populateEditor or createNewTemplate)
            const modalTitle = document.getElementById('editorModalTitle');
            const editorTitle = document.getElementById('editorTitle');
            if (modalTitle && editorTitle) {
                modalTitle.textContent = editorTitle.textContent;
            } else if (modalTitle && currentTemplate) {
                modalTitle.textContent = currentTemplate.template_name || 'Edit Template';
            }

            // Show modal
            const modal = new bootstrap.Modal(document.getElementById('editorModal'));
            modal.show();
        }

        function closeEditorModal() {
            // Close modal
            const modalElement = document.getElementById('editorModal');
            const modal = bootstrap.Modal.getInstance(modalElement);
            if (modal) {
                modal.hide();
            }

            // Reset state
            isCreatingNew = false;
            currentTemplate = null;

            // Refresh template list
            loadTemplatesByType(currentTemplateType);
        }

        function populateEditor(template) {
            console.log('Populating editor with template:', template);

            try {
                // Basic info (always shown)
                document.getElementById('editorTitle').textContent = template.template_name;
                document.getElementById('template_name').value = template.template_name || '';
                document.getElementById('description').value = template.description || '';

                // Hide all template field groups first
                document.querySelectorAll('.template-fields-group').forEach(group => {
                    group.style.display = 'none';
                });

                const templateType = template.template_type || currentTemplateType;

                // Show/hide Agent Type field based on template type
                const agentTypeField = document.getElementById('agent_type_field');
                if (templateType === 'theme' || templateType === 'narrative') {
                    agentTypeField.style.display = 'block';
                    document.getElementById('agent_type').value = template.agent_type || '';
                } else {
                    agentTypeField.style.display = 'none';
                }

                // Show appropriate fields based on template type
                if (templateType === 'theme' || templateType === 'narrative') {
                    // Theme & Narrative templates - show AI model settings
                    document.getElementById('theme-narrative-fields').style.display = 'block';

                    // Read from ai_config if available (new structure), fallback to old structure
                    const aiConfig = template.ai_config || {};
                    document.getElementById('model').value = aiConfig.model || template.model || 'gpt-4o';
                    document.getElementById('temperature').value = aiConfig.temperature || template.temperature || 0.8;
                    // max_tokens is NOT saved in template - it comes from ChannelConfig

                    // Sections - read from ai_config.sections (new) or template.sections (old)
                    const sections = aiConfig.sections || template.sections || {};
                    document.getElementById('role_definition').value = sections.role_definition || '';

                    // Core rules
                    renderCoreRules(sections.core_rules || []);

                    // Hook rules (only for narrative)
                    const hookRulesSection = document.getElementById('hookRulesSection');
                    if (templateType === 'narrative') {
                        hookRulesSection.style.display = 'block';
                        if (sections.hook_rules) {
                            document.getElementById('hook_enabled').checked = sections.hook_rules.enabled !== false;
                            document.getElementById('hook_style').value = sections.hook_rules.style || '';
                            document.getElementById('hook_length').value = sections.hook_rules.length || '';
                            document.getElementById('hook_placement').value = sections.hook_rules.placement || '';
                        }
                    } else {
                        hookRulesSection.style.display = 'none';
                    }

                    // SSML Settings - removed from narrative (now handled by TTS template)
                    document.getElementById('ssmlSettingsSection').style.display = 'none';

                } else if (templateType === 'image') {
                    // Image Generation template
                    document.getElementById('image-fields').style.display = 'block';

                    const aiConfig = template.ai_config || {};
                    const sections = aiConfig.sections || {};

                    // AI Config fields
                    document.getElementById('image_role_definition').value = sections.role_definition || 'You are a Visual Director for visual storytelling content.\nYou create highly detailed, cinematic image prompts that capture atmosphere and emotion.\nYour prompts are painterly, evocative, and designed for AI image generation.';



                    // Provider field
                    const provider = template.provider || 'ec2-sd35';
                    document.getElementById('image_provider').value = provider;

                    // Load core rules
                    loadImageCoreRules(sections.core_rules || [
                        'Always include specific lighting details (golden hour, dramatic shadows, rim lighting)',
                        'Focus on composition (rule of thirds, leading lines, depth of field)',
                        'Add atmospheric elements (mist, dust particles, light rays, weather)',
                        'Specify camera angle and shot type (wide shot, close-up, aerial view)',
                        'Include style keywords (cinematic, photorealistic, detailed textures)'
                    ]);

                    // Hide Hook/SSML sections
                    document.getElementById('hookRulesSection').style.display = 'none';
                    document.getElementById('ssmlSettingsSection').style.display = 'none';

                                } else if (templateType === 'video') {
                    document.getElementById('video-fields').style.display = 'block';

                    const editingParams = template.editing_params || {};

                    // Transitions
                    loadTransitions(editingParams.transitions || [{type: 'fade', duration: 0.5}]);

                    // Audio Params
                    const audioParams = editingParams.audio_params || {};
                    document.getElementById('audio_voice_volume').value = audioParams.voice_volume || 1.0;
                    document.getElementById('audio_music_volume').value = audioParams.music_volume || 0.3;
                    document.getElementById('audio_sfx_volume').value = audioParams.sfx_volume || 0.5;
                    document.getElementById('audio_normalize').checked = audioParams.normalize !== false;

                    // Export Settings
                    const exportSettings = editingParams.export_settings || {};
                    document.getElementById('export_resolution').value = exportSettings.resolution || '1920x1080';
                    document.getElementById('export_fps').value = exportSettings.fps || 30;
                    document.getElementById('export_codec').value = exportSettings.codec || 'libx264';
                    document.getElementById('export_bitrate').value = exportSettings.bitrate || '8M';

                    // Visual Effects
                    const visualEffects = editingParams.visual_effects || {};
                    document.getElementById('visual_effects_enabled').checked = visualEffects.enabled !== false;

                    const kenBurns = (visualEffects.effects || {}).ken_burns || {};
                    document.getElementById('ken_burns_enabled').checked = kenBurns.enabled !== false;
                    document.getElementById('ken_burns_zoom').value = kenBurns.zoom_factor || 1.1;

                    // Thumbnail Intro
                    document.getElementById('thumbnail_intro_enabled').checked = editingParams.enable_thumbnail_intro || false;
                    document.getElementById('thumbnail_intro_duration').value = editingParams.thumbnail_intro_duration_seconds || 3;

                    // Hide Hook/SSML sections
                    document.getElementById('hookRulesSection').style.display = 'none';
                    document.getElementById('ssmlSettingsSection').style.display = 'none';
                } else if (templateType === 'cta') {
                    document.getElementById('cta-fields').style.display = 'block';
                    const ctaSettings = template.cta_settings || {};

                    // CTA Configuration
                    document.getElementById('cta_type').value = ctaSettings.type || 'subscribe';
                    document.getElementById('cta_position').value = ctaSettings.position || 'end';
                    document.getElementById('cta_duration').value = ctaSettings.duration || 5;

                    // Visual Layout
                    document.getElementById('cta_bg_type').value = ctaSettings.bg_type || 'gradient';
                    document.getElementById('cta_bg_colors').value = ctaSettings.bg_colors || '#667eea, #764ba2';
                    document.getElementById('cta_text_align').value = ctaSettings.text_align || 'center';

                    // Content Elements
                    document.getElementById('cta_main_text').value = ctaSettings.main_text || "👻 Subscribe for more chilling stories";
                    document.getElementById('cta_secondary_text').value = ctaSettings.secondary_text || '';
                    if (ctaSettings.show_button !== undefined) {
                        document.getElementById('cta_show_button').checked = ctaSettings.show_button;
                    }
                    document.getElementById('cta_button_text').value = ctaSettings.button_text || 'SUBSCRIBE';
                    document.getElementById('cta_button_color').value = ctaSettings.button_color || '#ff0000';

                    // Animation
                    document.getElementById('cta_entrance_anim').value = ctaSettings.entrance_anim || 'fade-in';
                    document.getElementById('cta_exit_anim').value = ctaSettings.exit_anim || 'fade-out';

                    // Voice-over (if fields exist)
                    const voiceoverCheckbox = document.getElementById('cta_enable_voiceover');
                    const voiceoverText = document.getElementById('cta_voiceover_text');
                    if (voiceoverCheckbox && ctaSettings.enable_voiceover !== undefined) {
                        voiceoverCheckbox.checked = ctaSettings.enable_voiceover;
                    }
                    if (voiceoverText) {
                        voiceoverText.value = ctaSettings.voiceover_text || '';
                    }

                    // Hide Hook/SSML sections
                    document.getElementById('hookRulesSection').style.display = 'none';
                    document.getElementById('ssmlSettingsSection').style.display = 'none';

                } else if (templateType === 'tts') {
                    document.getElementById('tts-fields').style.display = 'block';
                    const ttsSettings = template.tts_settings || {};

                    // TTS Service & Engine
                    document.getElementById('tts_service').value = ttsSettings.tts_service || 'aws_polly_neural';
                    document.getElementById('tts_voice_profile').value = ttsSettings.tts_voice_profile || 'neutral_male';

                    // SSML Prosody
                    document.getElementById('tts_prosody_rate').value = ttsSettings.prosody_rate || 'medium';
                    document.getElementById('tts_prosody_pitch').value = ttsSettings.prosody_pitch || 'medium';
                    document.getElementById('tts_prosody_volume').value = ttsSettings.prosody_volume || 'medium';

                    // SSML Pauses & Breaks
                    document.getElementById('tts_break_sentence').value = ttsSettings.break_sentence || '300ms';
                    document.getElementById('tts_break_paragraph').value = ttsSettings.break_paragraph || '600ms';
                    document.getElementById('tts_break_scene').value = ttsSettings.break_scene || '1000ms';
                    document.getElementById('tts_break_dramatic').value = ttsSettings.break_dramatic || '1500ms';
                    document.getElementById('tts_break_strength').value = ttsSettings.break_strength || 'medium';

                    // SSML Emphasis
                    document.getElementById('tts_emphasis_level').value = ttsSettings.emphasis_level || 'moderate';
                    document.getElementById('tts_narrative_pace').value = ttsSettings.narrative_pace || 'medium';

                    // SSML Validation & Options
                    if (ttsSettings.auto_fix_ssml !== undefined) {
                        document.getElementById('tts_auto_fix_ssml').checked = ttsSettings.auto_fix_ssml;
                    } else {
                        document.getElementById('tts_auto_fix_ssml').checked = true; // default
                    }
                    if (ttsSettings.validate_strict !== undefined) {
                        document.getElementById('tts_validate_strict').checked = ttsSettings.validate_strict;
                    }
                    document.getElementById('tts_mood_variants').value = ttsSettings.tts_mood_variants || '';

                    // Audio Output Settings
                    document.getElementById('tts_output_format').value = ttsSettings.output_format || 'mp3';
                    document.getElementById('tts_sample_rate').value = ttsSettings.sample_rate || '24000';
                    document.getElementById('tts_language_code').value = ttsSettings.language_code || 'en-US';

                    // Hide Hook/SSML sections
                    document.getElementById('hookRulesSection').style.display = 'none';
                    document.getElementById('ssmlSettingsSection').style.display = 'none';

                } else if (templateType === 'sfx') {
                    document.getElementById('sfx-fields').style.display = 'block';
                    const aiConfig = template.ai_config || {};
                    const sections = aiConfig.sections || {};

                    // AI Configuration
                    document.getElementById('sfx_model').value = aiConfig.model || 'gpt-4o-mini';
                    document.getElementById('sfx_temperature').value = aiConfig.temperature || 0.7;
                    document.getElementById('sfx_max_tokens').value = aiConfig.max_tokens || 3000;
                    document.getElementById('sfx_role_definition').value = sections.role_definition || '';

                    // Core Rules
                    const coreRules = sections.core_rules || [];
                    renderSfxCoreRules(coreRules);

                    // Timing Rules
                    const timingRules = template.timing_rules || {};
                    document.getElementById('sfx_max_per_scene').value = timingRules.max_per_scene || 3;
                    document.getElementById('sfx_words_per_minute').value = timingRules.words_per_minute || 150;

                    // Hide Hook/SSML sections
                    document.getElementById('hookRulesSection').style.display = 'none';
                    document.getElementById('ssmlSettingsSection').style.display = 'none';

                } else if (templateType === 'thumbnail') {
                    document.getElementById('thumbnail-fields').style.display = 'block';
                    const aiConfig = template.ai_config || {};
                    const sections = aiConfig.sections || {};

                    // AI Configuration
                    document.getElementById('thumbnail_model').value = aiConfig.model || 'gpt-4o-mini';
                    document.getElementById('thumbnail_temperature').value = aiConfig.temperature || 0.8;
                    document.getElementById('thumbnail_max_tokens').value = aiConfig.max_tokens || 2000;
                    document.getElementById('thumbnail_role_definition').value = sections.role_definition || '';

                    // Core Rules
                    const coreRules = sections.core_rules || [];
                    renderThumbnailCoreRules(coreRules);

                    // Thumbnail Settings
                    document.getElementById('thumbnail_aspect_ratio').value = template.aspect_ratio || '16:9';
                    document.getElementById('thumbnail_resolution').value = template.resolution || '1280x720';
                    document.getElementById('thumbnail_text_overlay').value = template.text_overlay_enabled !== undefined ? template.text_overlay_enabled.toString() : 'true';
                    document.getElementById('thumbnail_max_text_words').value = template.max_text_words || 5;
                    document.getElementById('thumbnail_font_size_range').value = template.font_size_range || '80-120';

                    // Typography Rules
                    const typographyRules = template.typography_rules || {};
                    document.getElementById('thumbnail_recommended_fonts').value = typographyRules.recommended_fonts || 'Impact, Bebas Neue, Anton Bold';
                    document.getElementById('thumbnail_stroke_required').checked = typographyRules.stroke_required !== false;
                    document.getElementById('thumbnail_shadow_required').checked = typographyRules.shadow_required !== false;

                    // Composition Rules
                    const compositionRules = template.composition_rules || {};
                    document.getElementById('thumbnail_rule_of_thirds').checked = compositionRules.rule_of_thirds !== false;
                    document.getElementById('thumbnail_focal_points').value = compositionRules.focal_points || 'center_dominant, left_third_right_text';

                    // Hide Hook/SSML sections
                    document.getElementById('hookRulesSection').style.display = 'none';
                    document.getElementById('ssmlSettingsSection').style.display = 'none';

                } else if (templateType === 'description') {
                    document.getElementById('description-fields').style.display = 'block';
                    const aiConfig = template.ai_config || {};
                    const sections = aiConfig.sections || {};

                    // AI Configuration
                    document.getElementById('description_model').value = aiConfig.model || 'gpt-4o-mini';
                    document.getElementById('description_temperature').value = aiConfig.temperature || 0.7;
                    document.getElementById('description_max_tokens').value = aiConfig.max_tokens || 2000;
                    document.getElementById('description_role_definition').value = sections.role_definition || '';

                    // Core Rules
                    const coreRules = sections.core_rules || [];
                    renderDescriptionCoreRules(coreRules);

                    // SEO Keywords
                    const seoKeywords = template.seo_keywords || [];
                    document.getElementById('description_seo_keywords').value = Array.isArray(seoKeywords) ? seoKeywords.join(', ') : seoKeywords;

                    // Description Structure
                    const structure = template.structure || {};
                    document.getElementById('description_hook_max_length').value = structure.hook_max_length || 150;
                    document.getElementById('description_total_max_length').value = structure.total_max_length || 5000;
                    document.getElementById('description_include_timestamps').checked = structure.include_timestamps !== false;
                    document.getElementById('description_include_hashtags').checked = structure.include_hashtags !== false;
                    document.getElementById('description_max_hashtags').value = structure.max_hashtags || 5;

                    // Social Links
                    document.getElementById('description_social_links').value = template.social_links_template || '';

                    // Hide Hook/SSML sections
                    document.getElementById('hookRulesSection').style.display = 'none';
                    document.getElementById('ssmlSettingsSection').style.display = 'none';
                }

                // Show/hide delete buttons based on whether template is default
                const deleteBtn = document.getElementById('deleteBtn');
                const deleteBtnModal = document.getElementById('deleteBtnModal');

                if (template.is_default === true) {
                    deleteBtn.style.display = 'none';
                    if (deleteBtnModal) deleteBtnModal.style.display = 'none';
                } else {
                    deleteBtn.style.display = 'inline-block';
                    if (deleteBtnModal) deleteBtnModal.style.display = 'inline-block';
                }

                console.log('Editor populated successfully for type:', templateType);
            } catch (error) {
                console.error('Error populating editor:', error);
                showToast('Error loading template data: ' + error.message, 'danger');
            }
        }

        function updateImageServiceOptions() {
            const provider = document.getElementById('image_provider').value;
            const serviceSelect = document.getElementById('image_service');

            // Update service options based on provider
            if (provider.startsWith('ec2-sd35') || provider.startsWith('ec2-flux') || provider.startsWith('replicate-flux')) {
                serviceSelect.innerHTML = `
                    <option value="sd35-medium">SD 3.5 Medium (28 steps)</option>
                    <option value="flux-schnell">FLUX Schnell (4 steps, fast)</option>
                    <option value="flux-dev">FLUX Dev (higher quality)</option>
                `;
            } else if (provider.startsWith('aws-bedrock-sdxl')) {
                serviceSelect.innerHTML = `
                    <option value="sdxl-1.0">Stable Diffusion XL 1.0</option>
                `;
            } else if (provider.startsWith('aws-bedrock-nova')) {
                serviceSelect.innerHTML = `
                    <option value="nova-canvas-1024">Nova Canvas 1024x1024</option>
                    <option value="nova-canvas-2048">Nova Canvas 2048x2048</option>
                `;
            }
        }

        function renderCoreRules(rules) {
            const container = document.getElementById('coreRulesList');
            container.innerHTML = rules.map((rule, index) => `
                <div class="rule-item">
                    <input type="text" class="form-control" value="${rule}" data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `).join('');
        }

        function addCoreRule() {
            const container = document.getElementById('coreRulesList');
            const index = container.children.length;
            const ruleHtml = `
                <div class="rule-item">
                    <input type="text" class="form-control" placeholder="Enter rule..." data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `;
            container.insertAdjacentHTML('beforeend', ruleHtml);
        }

        function removeCoreRule(index) {
            const container = document.getElementById('coreRulesList');
            container.children[index].remove();
        }

        function getCoreRules() {
            const inputs = document.querySelectorAll('#coreRulesList input');
            return Array.from(inputs)
                .map(input => input.value.trim())
                .filter(value => value.length > 0);
        }

        // ===== SFX Template Core Rules Functions =====
        function renderSfxCoreRules(rules) {
            const container = document.getElementById('sfxCoreRulesContainer');
            container.innerHTML = rules.map((rule, index) => `
                <div class="rule-item">
                    <input type="text" class="form-control" value="${rule}" data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeSfxCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `).join('');
        }

        function addSfxCoreRule() {
            const container = document.getElementById('sfxCoreRulesContainer');
            const index = container.children.length;
            const ruleHtml = `
                <div class="rule-item">
                    <input type="text" class="form-control" placeholder="Enter SFX rule..." data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeSfxCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `;
            container.insertAdjacentHTML('beforeend', ruleHtml);
        }

        function removeSfxCoreRule(index) {
            const container = document.getElementById('sfxCoreRulesContainer');
            container.children[index].remove();
        }

        function getSfxCoreRules() {
            const inputs = document.querySelectorAll('#sfxCoreRulesContainer input');
            return Array.from(inputs)
                .map(input => input.value.trim())
                .filter(value => value.length > 0);
        }

        // ===== Thumbnail Template Core Rules Functions =====
        function renderThumbnailCoreRules(rules) {
            const container = document.getElementById('thumbnailCoreRulesContainer');
            container.innerHTML = rules.map((rule, index) => `
                <div class="rule-item">
                    <input type="text" class="form-control" value="${rule}" data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeThumbnailCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `).join('');
        }

        function addThumbnailCoreRule() {
            const container = document.getElementById('thumbnailCoreRulesContainer');
            const index = container.children.length;
            const ruleHtml = `
                <div class="rule-item">
                    <input type="text" class="form-control" placeholder="Enter thumbnail rule..." data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeThumbnailCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `;
            container.insertAdjacentHTML('beforeend', ruleHtml);
        }

        function removeThumbnailCoreRule(index) {
            const container = document.getElementById('thumbnailCoreRulesContainer');
            container.children[index].remove();
        }

        function getThumbnailCoreRules() {
            const inputs = document.querySelectorAll('#thumbnailCoreRulesContainer input');
            return Array.from(inputs)
                .map(input => input.value.trim())
                .filter(value => value.length > 0);
        }

        // ===== Description Template Core Rules Functions =====
        function renderDescriptionCoreRules(rules) {
            const container = document.getElementById('descriptionCoreRulesContainer');
            container.innerHTML = rules.map((rule, index) => `
                <div class="rule-item">
                    <input type="text" class="form-control" value="${rule}" data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeDescriptionCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `).join('');
        }

        function addDescriptionCoreRule() {
            const container = document.getElementById('descriptionCoreRulesContainer');
            const index = container.children.length;
            const ruleHtml = `
                <div class="rule-item">
                    <input type="text" class="form-control" placeholder="Enter description rule..." data-rule-index="${index}">
                    <button type="button" class="btn-remove-rule" onclick="removeDescriptionCoreRule(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </div>
            `;
            container.insertAdjacentHTML('beforeend', ruleHtml);
        }

        function removeDescriptionCoreRule(index) {
            const container = document.getElementById('descriptionCoreRulesContainer');
            container.children[index].remove();
        }

        function getDescriptionCoreRules() {
            const inputs = document.querySelectorAll('#descriptionCoreRulesContainer input');
            return Array.from(inputs)
                .map(input => input.value.trim())
                .filter(value => value.length > 0);
        }

        function toggleSSMLOptions() {
            const enabled = document.getElementById('use_ssml').checked;
            const optionsContent = document.getElementById('ssmlOptionsContent');
            optionsContent.style.display = enabled ? 'block' : 'none';
        }


        async function saveTemplate() {
            if (!currentTemplate) {
                showToast('No template loaded', 'warning');
                return;
            }

            // Validate required fields
            const templateName = document.getElementById('template_name').value.trim();
            if (!templateName) {
                showToast('Template name is required', 'warning');
                return;
            }

            // Build template data based on template type
            // Note: Templates are ALWAYS active (no draft mode for templates)
            let templateData = {
                template_type: currentTemplateType,
                template_name: templateName,
                is_active: 1, // Always active
                description: document.getElementById('description').value
            };

            // Add type-specific fields
            if (currentTemplateType === 'theme' || currentTemplateType === 'narrative') {
                templateData.agent_type = document.getElementById('agent_type').value;
                
                // Save in ai_config structure (new format)
                // Note: max_tokens is NOT saved here - it comes from ChannelConfig (max 16000)
                templateData.ai_config = {
                    model: document.getElementById('model').value,
                    temperature: parseFloat(document.getElementById('temperature').value),
                    sections: {
                        role_definition: document.getElementById('role_definition').value,
                        core_rules: getCoreRules(),
                        hook_rules: {
                            enabled: document.getElementById('hook_enabled').checked,
                            style: document.getElementById('hook_style').value,
                            length: document.getElementById('hook_length').value,
                            placement: document.getElementById('hook_placement').value
                        }
                    }
                };

                // SSML Settings removed from narrative (now handled by TTS template)
            } else if (currentTemplateType === 'image') {
                // Image Generation Template - save AI config
                templateData.ai_config = {
                    sections: {
                        role_definition: document.getElementById('image_role_definition').value,
                        core_rules: getImageCoreRules(),
                        output_schema: {} // Auto-generated
                    }
                };
                        } else if (currentTemplateType === 'video') {
                // Video Template - save editing_params (NEW STRUCTURE)
                templateData.editing_params = {
                    transitions: getTransitions(),
                    audio_params: {
                        voice_volume: parseFloat(document.getElementById('audio_voice_volume').value),
                        music_volume: parseFloat(document.getElementById('audio_music_volume').value),
                        sfx_volume: parseFloat(document.getElementById('audio_sfx_volume').value),
                        normalize: document.getElementById('audio_normalize').checked
                    },
    
                    export_settings: {
                        resolution: document.getElementById('export_resolution').value,
                        fps: parseInt(document.getElementById('export_fps').value),
                        codec: document.getElementById('export_codec').value,
                        bitrate: document.getElementById('export_bitrate').value
                    },
                    visual_effects: {
                        enabled: document.getElementById('visual_effects_enabled').checked,
                        effects: {
                            ken_burns: {
                                enabled: document.getElementById('ken_burns_enabled').checked,
                                zoom_factor: parseFloat(document.getElementById('ken_burns_zoom').value)
                            }
                        }
                    },
                    enable_thumbnail_intro: document.getElementById('thumbnail_intro_enabled').checked,
                    thumbnail_intro_duration_seconds: parseInt(document.getElementById('thumbnail_intro_duration').value)
                };
            } else if (currentTemplateType === 'cta') {
                // CTA Template - save CTA settings
                templateData.cta_settings = {
                    // CTA Configuration
                    type: document.getElementById('cta_type').value,
                    position: document.getElementById('cta_position').value,
                    duration: parseInt(document.getElementById('cta_duration').value),

                    // Visual Layout
                    bg_type: document.getElementById('cta_bg_type').value,
                    bg_colors: document.getElementById('cta_bg_colors').value,
                    text_align: document.getElementById('cta_text_align').value,

                    // Content Elements
                    main_text: document.getElementById('cta_main_text').value,
                    secondary_text: document.getElementById('cta_secondary_text').value,
                    show_button: document.getElementById('cta_show_button').checked,
                    button_text: document.getElementById('cta_button_text').value,
                    button_color: document.getElementById('cta_button_color').value,

                    // Animation
                    entrance_anim: document.getElementById('cta_entrance_anim').value,
                    exit_anim: document.getElementById('cta_exit_anim').value,

                    // Voice-over
                    enable_voiceover: document.getElementById('cta_enable_voiceover').checked,
                    voiceover_text: document.getElementById('cta_voiceover_text').value
                };
            } else if (currentTemplateType === 'tts') {
                // TTS Template - save TTS settings (AWS Polly + SSML)
                templateData.tts_settings = {
                    // TTS Service & Engine
                    tts_service: document.getElementById('tts_service').value,
                    tts_voice_profile: document.getElementById('tts_voice_profile').value,

                    // SSML Prosody (rate, pitch, volume)
                    prosody_rate: document.getElementById('tts_prosody_rate').value,
                    prosody_pitch: document.getElementById('tts_prosody_pitch').value,
                    prosody_volume: document.getElementById('tts_prosody_volume').value,

                    // SSML Pauses & Breaks
                    break_sentence: document.getElementById('tts_break_sentence').value,
                    break_paragraph: document.getElementById('tts_break_paragraph').value,
                    break_scene: document.getElementById('tts_break_scene').value,
                    break_dramatic: document.getElementById('tts_break_dramatic').value,
                    break_strength: document.getElementById('tts_break_strength').value,

                    // SSML Emphasis
                    emphasis_level: document.getElementById('tts_emphasis_level').value,
                    narrative_pace: document.getElementById('tts_narrative_pace').value,

                    // SSML Validation & Options
                    auto_fix_ssml: document.getElementById('tts_auto_fix_ssml').checked,
                    validate_strict: document.getElementById('tts_validate_strict').checked,
                    tts_mood_variants: document.getElementById('tts_mood_variants').value,

                    // Audio Output Settings
                    output_format: document.getElementById('tts_output_format').value,
                    sample_rate: document.getElementById('tts_sample_rate').value,
                    language_code: document.getElementById('tts_language_code').value
                };

            } else if (currentTemplateType === 'sfx') {
                // SFX Template - save ai_config + libraries + timing rules
                templateData.ai_config = {
                    model: document.getElementById('sfx_model').value,
                    temperature: parseFloat(document.getElementById('sfx_temperature').value),
                    max_tokens: parseInt(document.getElementById('sfx_max_tokens').value),
                    sections: {
                        role_definition: document.getElementById('sfx_role_definition').value,
                        core_rules: getSfxCoreRules()
                    }
                };

                // Note: sfx_library and music_library are auto-updated from S3, not edited here
                // Timing Rules
                templateData.timing_rules = {
                    max_per_scene: parseInt(document.getElementById('sfx_max_per_scene').value),
                    words_per_minute: parseInt(document.getElementById('sfx_words_per_minute').value)
                };

            } else if (currentTemplateType === 'thumbnail') {
                // Thumbnail Template - save ai_config + thumbnail settings + typography + composition
                templateData.ai_config = {
                    model: document.getElementById('thumbnail_model').value,
                    temperature: parseFloat(document.getElementById('thumbnail_temperature').value),
                    max_tokens: parseInt(document.getElementById('thumbnail_max_tokens').value),
                    sections: {
                        role_definition: document.getElementById('thumbnail_role_definition').value,
                        core_rules: getThumbnailCoreRules()
                    }
                };

                // Thumbnail Settings
                templateData.aspect_ratio = document.getElementById('thumbnail_aspect_ratio').value;
                templateData.resolution = document.getElementById('thumbnail_resolution').value;
                templateData.text_overlay_enabled = document.getElementById('thumbnail_text_overlay').value === 'true';
                templateData.max_text_words = parseInt(document.getElementById('thumbnail_max_text_words').value);
                templateData.font_size_range = document.getElementById('thumbnail_font_size_range').value;

                // Typography Rules
                templateData.typography_rules = {
                    recommended_fonts: document.getElementById('thumbnail_recommended_fonts').value,
                    stroke_required: document.getElementById('thumbnail_stroke_required').checked,
                    shadow_required: document.getElementById('thumbnail_shadow_required').checked
                };

                // Composition Rules
                templateData.composition_rules = {
                    rule_of_thirds: document.getElementById('thumbnail_rule_of_thirds').checked,
                    focal_points: document.getElementById('thumbnail_focal_points').value
                };

            } else if (currentTemplateType === 'description') {
                // Description Template - save ai_config + SEO + structure + social links
                templateData.ai_config = {
                    model: document.getElementById('description_model').value,
                    temperature: parseFloat(document.getElementById('description_temperature').value),
                    max_tokens: parseInt(document.getElementById('description_max_tokens').value),
                    sections: {
                        role_definition: document.getElementById('description_role_definition').value,
                        core_rules: getDescriptionCoreRules()
                    }
                };

                // SEO Keywords - convert comma-separated string to array
                const seoKeywordsText = document.getElementById('description_seo_keywords').value;
                templateData.seo_keywords = seoKeywordsText.split(',').map(k => k.trim()).filter(k => k.length > 0);

                // Description Structure
                templateData.structure = {
                    hook_max_length: parseInt(document.getElementById('description_hook_max_length').value),
                    total_max_length: parseInt(document.getElementById('description_total_max_length').value),
                    include_timestamps: document.getElementById('description_include_timestamps').checked,
                    include_hashtags: document.getElementById('description_include_hashtags').checked,
                    max_hashtags: parseInt(document.getElementById('description_max_hashtags').value)
                };

                // Social Links Template
                templateData.social_links_template = document.getElementById('description_social_links').value;
            }

            // Determine if creating new or updating existing
            const isNewTemplate = isCreatingNew || !currentTemplate.template_id;
            const method = isNewTemplate ? 'POST' : 'PUT';
            const url = isNewTemplate
                ? `${API_BASE}/`
                : `${API_BASE}/template/${currentTemplate.template_id}`;

            console.log(`${method} ${isNewTemplate ? 'new' : 'existing'} template:`, templateData);

            try {
                const response = await fetch(url, {
                    method: method,
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(templateData)
                });

                const data = await response.json();
                console.log('Save response:', data);

                if (data.success) {
                    showToast(
                        isNewTemplate ? 'Template created successfully!' : 'Template saved successfully!',
                        'success'
                    );

                    // Close modal and refresh
                    closeEditorModal();
                } else {
                    showToast('Error saving template: ' + (data.error || 'Unknown error'), 'danger');
                }
            } catch (error) {
                console.error('Error saving template:', error);
                showToast('Failed to save template: ' + error.message, 'danger');
            }
        }

        async function deleteTemplate() {
            if (!currentTemplate || !currentTemplate.template_id) {
                showToast('No template selected', 'warning');
                return;
            }

            // Prevent deletion of base templates
            const protectedTemplates = ['theme_agent', 'narrative_architect'];
            if (protectedTemplates.includes(currentTemplate.template_id)) {
                showToast('Cannot delete base template', 'danger');
                return;
            }

            // Confirmation dialog
            const confirmMsg = `Are you sure you want to delete template "${currentTemplate.template_name}"?\n\nThis action cannot be undone.`;
            if (!confirm(confirmMsg)) {
                return;
            }

            console.log('Deleting template:', currentTemplate.template_id);

            try {
                const url = `${API_BASE}/template/${currentTemplate.template_id}?type=${currentTemplateType}`;
                console.log('DELETE to:', url);

                const response = await fetch(url, {
                    method: 'DELETE',
                    headers: { 'Content-Type': 'application/json' }
                });

                const data = await response.json();
                console.log('Delete response:', data);

                if (data.success) {
                    showToast('Template deleted successfully!', 'success');
                    backToLibrary();
                    loadTemplates();  // Refresh the template list
                } else {
                    showToast('Error deleting template: ' + (data.error || 'Unknown error'), 'danger');
                }
            } catch (error) {
                console.error('Error deleting template:', error);
                showToast('Failed to delete template: ' + error.message, 'danger');
            }
        }

        async function cloneTemplate(templateId) {
            const newName = prompt('Enter name for cloned template:');
            if (!newName) return;

            try {
                // Get the original template
                const getResponse = await fetch(`${API_BASE}/template/${templateId}?type=${currentTemplateType}`);
                const getData = await getResponse.json();

                if (!getData.success) {
                    showToast('Error loading template to clone', 'danger');
                    return;
                }

                const original = getData.data.template;

                // Create new template with cloned data
                const newTemplate = {
                    ...original,
                    template_name: newName,
                    template_type: currentTemplateType,
                    created_at: Date.now(),
                    updated_at: Date.now()
                };
                delete newTemplate.template_id; // Let backend generate new ID
                delete newTemplate.version; // Reset version

                const response = await fetch(API_BASE, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(newTemplate)
                });

                const data = await response.json();

                if (data.success) {
                    showToast('Template cloned successfully!', 'success');
                    loadTemplatesByType(currentTemplateType);
                } else {
                    showToast('Error cloning template: ' + (data.error || 'Unknown error'), 'danger');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('Failed to clone template', 'danger');
            }
        }

        // ====================================================================
        // PREVIEW FUNCTIONS
        // ====================================================================

        async function loadChannels() {
            // TODO: Load channels from API
            // For now, mock data
            allChannels = [
                { channelId: 'channel1', channel_name: 'Horror Stories' },
                { channelId: 'channel2', channel_name: 'Mystery Tales' }
            ];

            const select = document.getElementById('previewChannelSelect');
            select.innerHTML = '<option value="">Select Channel...</option>' +
                allChannels.map(c => `<option value="${c.channelId}">${c.channel_name}</option>`).join('');
        }

        async function generatePreview() {
            const channelId = document.getElementById('previewChannelSelect').value;
            if (!channelId) {
                showToast('Please select a channel', 'warning');
                return;
            }

            const container = document.getElementById('previewContainer');
            container.innerHTML = '<div class="text-center py-5"><div class="loading-spinner"></div></div>';

            try {
                const response = await fetch(`${API_BASE}/${currentTemplate.template_id}/preview?channel_id=${channelId}&topic=Sample Topic`);
                const data = await response.json();

                if (data.success) {
                    renderPreview(data.data.preview);
                } else {
                    showToast('Error generating preview', 'danger');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('Failed to generate preview', 'danger');
            }
        }

        function renderPreview(preview) {
            document.getElementById('previewContainer').innerHTML = `
                <div class="preview-container">
                    <div class="preview-section">
                        <div class="preview-label">SYSTEM PROMPT:</div>
                        <div class="preview-content">${escapeHtml(preview.system_prompt)}</div>
                    </div>
                    <div class="preview-section">
                        <div class="preview-label">USER MESSAGE:</div>
                        <div class="preview-content">${escapeHtml(preview.user_message)}</div>
                    </div>
                    <div class="preview-section">
                        <div class="preview-label">MODEL CONFIG:</div>
                        <div class="preview-content">${JSON.stringify(preview.model_config, null, 2)}</div>
                    </div>
                </div>
            `;
        }

        // ====================================================================
        // UTILITY FUNCTIONS
        // ====================================================================

        // showTab and hideTab functions removed - tabs no longer exist

        function backToLibrary() {
            closeEditorModal();
        }

        function showToast(message, type = 'info') {
            const toast = document.createElement('div');
            toast.className = `alert alert-${type} alert-dismissible fade show`;
            toast.innerHTML = `
                ${message}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            `;
            document.querySelector('.toast-container').appendChild(toast);

            setTimeout(() => toast.remove(), 5000);
        }

        async function showCreateModal() {
            createNewTemplate(currentTemplateType);
        }

        async function showCreateModal_OLD() {
            const templateName = prompt('Enter template name:', 'My Custom Template');
            if (!templateName) return;

            const agentType = prompt('Enter agent type (narrative_architect, theme_agent, or custom):', 'custom');
            if (!agentType) return;

            const description = prompt('Enter description:', 'Custom template for specific use case');

            // Generate unique agent_id
            const agentId = agentType === 'custom'
                ? `custom_${Date.now()}`
                : `${agentType}_custom_${Date.now()}`;

            const newTemplate = {
                template_type: currentTemplateType,
                template_name: templateName,
                agent_type: agentType,
                description: description || '',
                version: 1,
                is_active: true,
                model: 'gpt-4o',
                temperature: 0.8,
                max_tokens: 4000,
                sections: {
                    role_definition: 'You are an AI assistant...',
                    core_rules: [],
                    hook_rules: {
                        enabled: true,
                        style: 'engaging',
                        length: '50-120 characters',
                        placement: 'first_15_seconds'
                    },
                    ssml_rules: {
                        pauses: {
                            after_hook: '1500ms',
                            between_scenes: '1000ms'
                        }
                    },
                    variation_logic: {},
                    output_schema: {}
                },
                created_by: 'admin',
                genre: 'General'
            };

            try {
                const response = await fetch(API_BASE, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(newTemplate)
                });

                const data = await response.json();

                if (data.success) {
                    showToast('Template created successfully!', 'success');
                    loadTemplatesByType(currentTemplateType);
                } else {
                    showToast('Error creating template: ' + (data.error || 'Unknown error'), 'danger');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('Failed to create template', 'danger');
            }
        }

        // ====================================================================
        // ====================================================================
        // EC2 SD35 CONTROL FUNCTIONS
        // ====================================================================

        // EC2 SD35 CONTROL FUNCTIONS FOR PROMPTS EDITOR
        // Add these functions to prompts-editor.html replacing the old Vast.ai functions

        // ====================================================================
        // EC2 SD35 CONTROL FUNCTIONS
        // ====================================================================

        async function refreshEC2SD35Status() {
            const refreshBtn = document.getElementById('ec2SD35RefreshBtn');
            if (refreshBtn) {
                refreshBtn.textContent = '⏳ Refreshing...';
                refreshBtn.disabled = true;
            }

            try {
                const response = await fetch(`${EC2_SD35_API_URL}`, {
                    method: 'POST',
                    mode: 'cors',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ action: 'status' })
                });

                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
                }

                const result = await response.json();

                // Validate result exists
                if (!result) {
                    throw new Error('Empty response from EC2 control API');
                }

                // Handle both HTTP format ({statusCode, body}) and direct format
                const data = result.body
                    ? (typeof result.body === 'string' ? JSON.parse(result.body) : result.body)
                    : result;

                if (data && data.error) {
                    document.getElementById('ec2SD35Status').textContent = '❌ Error';
                    document.getElementById('ec2SD35InstanceId').textContent = data.error;
                    document.getElementById('ec2SD35PublicIp').textContent = '--';
                    document.getElementById('ec2SD35EndpointInfo').style.display = 'none';
                    return;
                }

                // Handle both 'status' and 'state' field names
                const status = data.status || data.state || 'unknown';
                const instanceId = data.instance_id || '--';
                const publicIp = data.public_ip || '--';
                const endpoint = data.endpoint || '--';

                const isRunning = status === 'running';
                const isStopped = status === 'stopped';

                // Update status
                const statusEl = document.getElementById('ec2SD35Status');
                if (ec2SD35UserAction === 'starting' && !isRunning) {
                    statusEl.textContent = '🟡 Starting...';
                    statusEl.style.color = '#f59e0b';
                } else if (ec2SD35UserAction === 'stopping' && !isStopped) {
                    statusEl.textContent = '🟡 Stopping...';
                    statusEl.style.color = '#f59e0b';
                } else if (isRunning) {
                    statusEl.textContent = '🟢 Running';
                    statusEl.style.color = '#10b981';
                    if (ec2SD35UserAction === 'starting') {
                        ec2SD35UserAction = null;
                    }
                } else if (isStopped) {
                    statusEl.textContent = '🔴 Stopped';
                    statusEl.style.color = '#ef4444';
                    if (ec2SD35UserAction === 'stopping') {
                        ec2SD35UserAction = null;
                    }
                } else {
                    statusEl.textContent = `⚠️ ${status}`;
                    statusEl.style.color = '#f59e0b';
                }

                // Update instance info
                document.getElementById('ec2SD35InstanceId').textContent = instanceId.substring(0, 19);
                document.getElementById('ec2SD35PublicIp').textContent = publicIp;

                // Update endpoint info
                const endpointDiv = document.getElementById('ec2SD35EndpointInfo');
                if (isRunning && endpoint && endpoint !== '--') {
                    endpointDiv.style.display = 'block';
                    document.getElementById('ec2SD35Endpoint').textContent = endpoint;
                } else {
                    endpointDiv.style.display = 'none';
                }

                // Update button states
                document.getElementById('ec2SD35StartBtn').disabled = isRunning || ec2SD35UserAction === 'starting';
                document.getElementById('ec2SD35StopBtn').disabled = isStopped || ec2SD35UserAction === 'stopping';

                // Auto-refresh management
                const shouldPoll = isRunning || ec2SD35UserAction === 'starting' || ec2SD35UserAction === 'stopping';

                if (shouldPoll && !ec2SD35StatusInterval) {
                    console.log('🔄 Starting EC2 auto-refresh');
                    ec2SD35StatusInterval = setInterval(refreshEC2SD35Status, 10000);  // Every 10 seconds
                } else if (!shouldPoll && ec2SD35StatusInterval) {
                    console.log('🛑 Stopping EC2 auto-refresh');
                    clearInterval(ec2SD35StatusInterval);
                    ec2SD35StatusInterval = null;
                }

                // Restore Refresh button
                if (refreshBtn) {
                    refreshBtn.textContent = '🔄 Refresh';
                    refreshBtn.disabled = false;
                }

            } catch (error) {
                console.error('❌ Error fetching EC2 SD35 status:', error);

                document.getElementById('ec2SD35Status').textContent = '⚠️ Connection Error';
                document.getElementById('ec2SD35Status').style.color = '#f59e0b';
                document.getElementById('ec2SD35InstanceId').textContent = error.message || 'Failed to fetch status';
                document.getElementById('ec2SD35PublicIp').textContent = '--';
                document.getElementById('ec2SD35EndpointInfo').style.display = 'none';

                if (refreshBtn) {
                    refreshBtn.textContent = '🔄 Refresh';
                    refreshBtn.disabled = false;
                }
            }
        }

        async function startEC2SD35Instance() {
            const btn = document.getElementById('ec2SD35StartBtn');
            btn.disabled = true;
            btn.textContent = '⏳ Starting...';

            try {
                console.log('Starting EC2 SD35 instance...');
                const response = await fetch(`${EC2_SD35_API_URL}`, {
                    method: 'POST',
                    mode: 'cors',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ action: 'start' })
                });

                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
                }

                const result = await response.json();

                // Validate result exists
                if (!result) {
                    throw new Error('Empty response from EC2 API');
                }

                // Handle both HTTP format ({statusCode, body}) and direct format
                const data = result.body
                    ? (typeof result.body === 'string' ? JSON.parse(result.body) : result.body)
                    : result;

                console.log('Start response:', data);

                if (data.status === 'running' || data.message) {
                    console.log('✅ Instance starting');

                    // Set user action state
                    ec2SD35UserAction = 'starting';

                    // Update UI immediately
                    document.getElementById('ec2SD35Status').textContent = '🟡 Starting...';
                    document.getElementById('ec2SD35Status').style.color = '#f59e0b';
                    document.getElementById('ec2SD35InstanceId').textContent = 'Booting...';

                    // Start polling
                    setTimeout(refreshEC2SD35Status, 5000);

                    // Re-enable button
                    setTimeout(() => {
                        btn.disabled = false;
                        btn.textContent = '▶️ Start Instance';
                    }, 2000);
                } else {
                    alert('Failed to start instance: ' + (data.error || 'Unknown error'));
                    btn.disabled = false;
                    btn.textContent = '▶️ Start Instance';
                }
            } catch (error) {
                console.error('Error starting instance:', error);
                alert('Failed to start instance: ' + error.message);
                btn.disabled = false;
                btn.textContent = '▶️ Start Instance';
            }
        }

        async function stopEC2SD35Instance() {
            if (!confirm('⚠️ Stop EC2 SD35 instance?\n\nThis will save money (~$1/hour) but the instance will need 3-5 minutes to restart.')) {
                return;
            }

            const btn = document.getElementById('ec2SD35StopBtn');
            btn.disabled = true;
            btn.textContent = '⏳ Stopping...';

            try {
                console.log('Stopping EC2 SD35 instance...');
                const response = await fetch(`${EC2_SD35_API_URL}`, {
                    method: 'POST',
                    mode: 'cors',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ action: 'stop' })
                });

                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
                }

                const result = await response.json();

                // Validate result exists
                if (!result) {
                    throw new Error('Empty response from EC2 API');
                }

                // Handle both HTTP format ({statusCode, body}) and direct format
                const data = result.body
                    ? (typeof result.body === 'string' ? JSON.parse(result.body) : result.body)
                    : result;

                console.log('Stop response:', data);

                if (data.status === 'stopping' || data.status === 'stopped' || data.message) {
                    console.log('✅ Instance stopping');

                    // Set user action state
                    ec2SD35UserAction = 'stopping';

                    // Update UI immediately
                    document.getElementById('ec2SD35Status').textContent = '🟡 Stopping...';
                    document.getElementById('ec2SD35Status').style.color = '#f59e0b';
                    document.getElementById('ec2SD35EndpointInfo').style.display = 'none';

                    // Start polling
                    setTimeout(refreshEC2SD35Status, 3000);

                    // Re-enable button
                    setTimeout(() => {
                        btn.disabled = false;
                        btn.textContent = '⏹️ Stop Instance';
                    }, 2000);
                } else {
                    alert('Failed to stop instance: ' + (data.error || 'Unknown error'));
                    btn.disabled = false;
                    btn.textContent = '⏹️ Stop Instance';
                }
            } catch (error) {
                console.error('Error stopping instance:', error);
                alert('Failed to stop instance: ' + error.message);
                btn.disabled = false;
                btn.textContent = '⏹️ Stop Instance';
            }
        }

        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                alert('📋 Copied to clipboard: ' + text);
            }).catch(err => {
                console.error('Failed to copy:', err);
            });
        }


        // UTILITY FUNCTIONS
        // ====================================================================

        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }
        // Image Template Core Rules Management
        // ========================================

        function addImageCoreRule(ruleText = '') {
            const container = document.getElementById('image_core_rules_container');
            const ruleDiv = document.createElement('div');
            ruleDiv.className = 'input-group mb-2';
            ruleDiv.innerHTML = `
                <input type="text" class="form-control" value="${ruleText}" placeholder="Enter a rule for AI image prompt generation...">
                <button type="button" class="btn btn-outline-danger" onclick="this.parentElement.remove()">
                    <i class="bi bi-trash"></i>
                </button>
            `;
            container.appendChild(ruleDiv);
        }

        function getImageCoreRules() {
            const container = document.getElementById('image_core_rules_container');
            const inputs = container.querySelectorAll('input[type="text"]');
            const rules = [];
            inputs.forEach(input => {
                if (input.value.trim()) {
                    rules.push(input.value.trim());
                }
            });
            return rules;
        }

        function loadImageCoreRules(rules) {
            const container = document.getElementById('image_core_rules_container');
            container.innerHTML = ''; // Clear existing

            if (rules && rules.length > 0) {
                rules.forEach(rule => addImageCoreRule(rule));
            } else {
                // Add default rules
                addImageCoreRule('Always include specific lighting details (golden hour, dramatic shadows)');
                addImageCoreRule('Focus on composition (rule of thirds, leading lines, depth)');
                addImageCoreRule('Add atmospheric elements (mist, dust particles, light rays)');
                addImageCoreRule('Specify camera angle and shot type (wide shot, close-up, aerial)');
                addImageCoreRule('Include style keywords (cinematic, photorealistic, detailed textures)');
            }
        }

            
        // ========================================
        // Video Template Transitions Management
        // ========================================

        function addTransition(type = 'fade', duration = 0.5) {
            const container = document.getElementById('transitions_container');
            const transitionDiv = document.createElement('div');
            transitionDiv.className = 'input-group mb-2';
            transitionDiv.innerHTML = `
                <select class="form-select" style="max-width: 200px;">
                    <option value="cut" ${type === 'cut' ? 'selected' : ''}>Cut (instant)</option>
                    <option value="fade" ${type === 'fade' ? 'selected' : ''}>Fade</option>
                    <option value="dissolve" ${type === 'dissolve' ? 'selected' : ''}>Dissolve</option>
                    <option value="wipe" ${type === 'wipe' ? 'selected' : ''}>Wipe</option>
                    <option value="slide" ${type === 'slide' ? 'selected' : ''}>Slide</option>
                </select>
                <span class="input-group-text">Duration:</span>
                <input type="number" class="form-control" style="max-width: 100px;" step="0.1" min="0" max="3" value="${duration}">
                <span class="input-group-text">sec</span>
                <button type="button" class="btn btn-outline-danger" onclick="this.parentElement.remove()">
                    <i class="bi bi-trash"></i>
                </button>
            `;
            container.appendChild(transitionDiv);
        }

        function getTransitions() {
            const container = document.getElementById('transitions_container');
            const items = container.querySelectorAll('.input-group');
            const transitions = [];

            items.forEach(item => {
                const type = item.querySelector('select').value;
                const duration = parseFloat(item.querySelector('input[type="number"]').value);
                transitions.push({type, duration});
            });

            // Always return at least one transition
            if (transitions.length === 0) {
                transitions.push({type: 'fade', duration: 0.5});
            }

            return transitions;
        }

        function loadTransitions(transitions) {
            const container = document.getElementById('transitions_container');
            container.innerHTML = ''; // Clear

            if (transitions && transitions.length > 0) {
                transitions.forEach(t => addTransition(t.type, t.duration));
            } else {
                addTransition('fade', 0.5);
            }
        }
    </script>
    <script src="js/navigation.js?v=20251130-0423"></script>
</body>
</html>
